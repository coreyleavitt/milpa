# Python build/runtime
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
*.egg-info/
build/
dist/

# Hypothesis's example replay database — a cache, not source of truth.
# Counterexamples worth keeping are checked in as regression example
# tests (see docs/rfc-property-based-testing.md §Counterexample lifecycle).
.hypothesis/

# Editor noise
.vscode/
.idea/
*.swp
.DS_Store

# Local-only env
.env
.venv/
venv/

# milpa's own consumed outputs in this repo's tests
_deps/
nim.cfg
