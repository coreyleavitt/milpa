{
  "description": "The Bearer challenge's realm is a non-http(s) scheme (file:///etc/passwd) -> fail closed, phase=token, before ever issuing a second (token) request. C1: a hostile/MITM registry that returns a file://, ftp://, or other non-http(s) realm must not be able to steer the client's second GET at that URL.",
  "exchanges": [
    {
      "method": "GET",
      "url": "https://ghcr.io/v2/",
      "response": {
        "status": 401,
        "headers": {
          "WWW-Authenticate": "Bearer realm=\"file:///etc/passwd\",service=\"ghcr.io\"",
          "Content-Length": "0"
        }
      }
    }
  ]
}
