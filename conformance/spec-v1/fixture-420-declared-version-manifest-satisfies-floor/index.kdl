// Phase-A enumeration only needs "bearssl" to exist in the index (chronos's
// named requirement triggers stub enumeration); this low version is never
// actually selected — Maxver strategy prefers the real git-materialized
// candidate (0.6.0, milpa.kdl-derived — see fixture note), which satisfies
// the >= 0.2.8 floor — so this stub is never materialised/fetched.
schema_version 1
package "bearssl" {
    version "0.2.8" {
        content_hash "dag-sha256:e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855"
        provenance {
            kind "git"
            url "https://example.com/bearssl-index-only.git"
            ref "v0.2.8"
            commit_sha "aaaa0000aaaa0000aaaa0000aaaa0000aaaa0000"
        }
    }
}
