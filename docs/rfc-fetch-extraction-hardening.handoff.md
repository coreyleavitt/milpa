# rfc-fetch-extraction-hardening — handoff

- **Stage:** 3 (tdd grind, via `/loop` self-paced). **11/11 slices done — GRIND COMPLETE.** All slices: H0, H1, H1b, H2, H-infra, H4, H3a, H3b, H3c, H3d, H5.
- **⚠️ PENDING USER ACTION (post-grind, needs network):** the object-store rewrite re-hashes any fresco dep whose checkout differed from committed blobs → re-lock the fresco integration fixture: `cd impls/python && MILPA_INTEGRATION_TESTS=1 uv run pytest tests/test_integration.py -v`; if it fails on a content_hash mismatch, re-lock via `MILPA_INTEGRATION_TESTS=1 milpa update` against fresco and re-pin the fixture. Corpus is unaffected.
- **Resume (stage 3 grind):**
  `/loop implement the next unimplemented slice of docs/rfc-fetch-extraction-hardening.md with /tdd, following the standing rules; after each slice report one progress line (e.g. "slice 4/11 done, 7 remaining"); stop when every slice is implemented`
- **Each slice is delegated to a sonnet subagent** (RED→GREEN→REFACTOR), control loop stays thin. Gates per slice: `cd impls/python && uv run pytest` + `./dev-rust test --workspace` (or `-p milpa-core`/`-p milpa-conformance`). No commits during the grind — left in the tree.

## Slices — round-2 reordered + H3 split
Order: H0 → H1 → H1b → H2 → **H-infra → H4 → H3a → H3b → H3c → H3d → H5**
- [x] H0 — verify #175 corrupt-archive parity (Rust passes fixture-291) + .zip magic-byte guard
- [x] H1 — #149 streaming download cap + `FETCH-DOWNLOAD-SIZE-EXCEEDED`. Python `make_http_get` now uses `Popen` + chunked read (aborts + kills curl at cap). Rust `with_curl` uses `curl_streaming_transport` (same pattern). Post-read safety net in both impls raises `FETCH-DOWNLOAD-SIZE-EXCEEDED` (not `FETCH-DOWNLOAD-FAILED`) for injected transports. Slug added to `spec/errors.md`, `errors.py`, and Rust `FetchError::all_codes()` in the same change. Bijection lint green in both impls.
- [x] H1b — bz2/xz bomb-guard parity (Rust) + Python decompressed-stream cap. Rust: bz2/xz guards already landed (decompress_capped + decompress_capped_xz). Added 4 new unit tests calling decompress_capped/decompress_capped_xz DIRECTLY with tiny caps (512 B) — proving the decompressor-level guard fires before extract_tar (not just post-extraction via safe_extract Limits). FIXTURE_BZ2_BOMB_1K added (103-byte bz2, decompresses to ~10 KiB; no system bzip2 required). Python: added _decompress_capped helper in safe_extract.py (gzip/bz2/xz magic-byte detection → pre-decompression with hard stream cap → EXTRACT-SIZE-LIMIT); added decomp_cap field to Limits (independent of max_total_size; default 1 GiB + 512 B matching Rust formula); updated extract_tar to always read bytes upfront and apply stream cap before tarfile. Lying-header test: archive with member.size=0 but 5 KB compressed-stream padding slips through member.size checks but fires stream cap. Reuses EXTRACT-SIZE-LIMIT; bijection lint green.
- [x] H2 — #144 hardlink/strip geometry, both impls. Copy-bytes materialisation (not symlink, not reject); two-pass extraction (hardlinks deferred to pass 2 for forward-reference safety); `strip_components` applied to `linkname` via POSIX-`/` split; escape → `EXTRACT-ZIP-SLIP` (no new slug). `EXTRACT-HARDLINK-UNSUPPORTED` (which was never in `spec/errors.md`) noted as superseded in `rfc-pluggable-fetchers.md`. Spec clause added to `spec/plugin-contract.md` §2.2. Corpus fixture `fixture-293-hardlink-copy-bytes` (crafted tar: hardlink forward-reference + strip_components=0; both impls pass). 5 Python unit tests + 4 Rust unit tests pin all 5 behaviors (H2a–e). Hash-stability confirmed: hardlink archive and plain-duplicate archive → same `content_hash`. Gates: Python 2171 passed; Rust workspace all ok.
- [x] H-infra — generated-bare-repo test TIER: runner integration in BOTH impls. `cmd=git-protocol` dispatches before `_build_env` (no `milpa.kdl`/`mocked-fetches/`); `git-protocol.json` declares repos + fetch; `expected/content_hash` is the single assertion. Python: `_make_git_protocol_repo` + `_execute_git_protocol_fixture` (real `GitFetcher`/`FetcherRegistry`/`compute_content_hash`). Rust: minimal `serde_like` JSON parser (no new crate), `make_git_protocol_repo` + `run_git_protocol_fixture` (real `fetch_git` + `compute_content_hash`). Smoke fixture `fixture-294-git-protocol-smoke` (`hello.nim` + `stub.nimble`, `content_hash=sha256:a828b53a2fcadc01bce623d4eba342db64be61a82589e3985359167a3f51020d`). Gate: Python 2172 passed; Rust 257 pass, 34 skip, 0 fail. H4/H3d/H5 extend schema by adding `commit_sha` (non-tip), `.gitattributes` files, and submodule repo entries respectively.
- [x] H4 — #145 shallow-clone 4-step convergence (Rust). **MOVED BEFORE H3** — H3's object reads need objects present locally; Rust lacks the fetch chain. Ported Python's `_ensure_commit_present` 4-step chain to Rust as `ensure_commit_present` in `impls/rust/crates/milpa-core/src/fetchers.rs`. Precision fix (a): `--unshallow` on a complete repo soft-fails only on "does not make sense"/"complete repository" error text; real fetch failures propagate as `FETCH-GIT-FAILED` in both impls (Python tightened from blanket swallow). Precision fix (b): confirmed both impls already do full-depth clone (no `--depth=1`); documented in comments. H-infra schema extended: `commits` array (multi-commit form, oldest-first) + `orphan_tip` object (creates orphan commit and force-resets branch, simulating a force-push so the earlier commit is unreachable from refs after `git clone`). SHA injection via `"commit_sha": "@repo:<name>:commit:<index>"` resolved at generation time in both runners. Fixture `fixture-295-git-protocol-non-tip-commit`: RED on Rust before fix (`FETCH-GIT-COMMIT-ABSENT`), GREEN after; passes Python throughout. Gates: Python 2173 passed; Rust workspace all ok (258 corpus pass, 34 skip).
- [x] H3a — spec first: rewrite `identity.md §1.6/§1.7` + add `materialize-git-tree` contract & structural-enforcement subsection to `plugin-contract.md`. §1.6 unchanged (line-ending rule intact). §1.7 completely rewritten: deleted `-c core.autocrlf=false -c core.filemode=false` clause; introduced §1.7.1–§1.7.6 (clone discipline, `ls-tree`+`cat-file --batch`, empty-dir non-synthesis, fixed on-disk modes, submodule identity/provenance split + always-on recursion, migration note scoped to integration fixture only). `plugin-contract.md` gains §2.3 (`materialize-git-tree` contract: subprocess discipline, LFS first-line detection→`FETCH-GIT-LFS-POINTER` named-not-yet-catalogued, per-symlink escape→`EXTRACT-SYMLINK-ESCAPE` reused, fixed modes, submodule recursion, output cleanliness) and §2.4 (structural-enforcement: §2.4.1 Normalized/Complete/Deterministic via VCS primitive, §2.4.2 Bounded at admission chokepoint, §2.4.3 archive fetchers MUST use `SafeExtractor`). `FETCH-GIT-LFS-POINTER` named in prose only; no `spec/errors.md` `### \`…\`` header added — lint stays green (bijection parses only errors.md headers). Gate: 2173 passed, 30 skipped.
- [x] H3b — Python `materialize_git_tree` (`--no-checkout`, `cat-file --batch`, fixed mode, **per-symlink escape→`EXTRACT-SYMLINK-ESCAPE`**, LFS first-line→`FETCH-GIT-LFS-POINTER`). `FETCH-GIT-LFS-POINTER` added to `spec/errors.md` and `errors.py` in the SAME change as its raise site — bijection green. `_GIT_TRANSPORT_FLAGS` now empty (checkout-normalization flags deleted; object-store path has no checkout). `GitFetcher.fetch` refactored to thin orchestration: `git clone --no-checkout` → ensure commit present → `materialize_git_tree` → return `GitReceipt`. `materialize_git_tree` is the single chokepoint: `git ls-tree -r <commit>`, ONE `git cat-file --batch` subprocess for all blobs, fixed modes (0o644/0o755), per-symlink lexical containment reusing `_normalize_lexical` from `safe_extract.py`, LFS first-line detection, H5 submodule recursion seam defined (param wired, H5 fills `.gitmodules` parsing). Clone scratch and output tree are separate directories; scratch cleaned up in `finally`. **No .git in output tree.** **content_hash churn: corpus does NOT churn** — `fixture-294` hash is unchanged (`sha256:a828b53a2fcadc01bce623d4eba342db64be61a82589e3985359167a3f51020d`); `MockedGitFetcher` stages verbatim bytes, no git invocation. H3b invariant tests (a–e) are **Python-only** in `tests/test_git_fetcher.py` (not promoted to shared corpus) to avoid turning Rust red — H3d promotes them once H3c lands. Rust: `FETCH-GIT-LFS-POINTER` added to `DEFERRED` in `corpus.rs` (H3c wires Rust impl). Gate: Python 2190 passed, 30 skipped; Rust workspace all ok.
- [x] H3c — Rust `materialize_git_tree` parity in `fetchers.rs`: `--no-checkout` clone, `ls-tree -r` + single `cat-file --batch` (parses `<sha> <type> <size>\n<bytes>\n` framed binary stream), fixed 0o644/0o755 modes, no empty dirs, per-symlink lexical containment→`EXTRACT-SYMLINK-ESCAPE` (reuses `safe_extract.rs` helper), LFS first-line→`FETCH-GIT-LFS-POINTER` (moved out of `DEFERRED` to a real raise site; `DEFERRED = &[]`; bijection clean). Checkout path deleted. All a–e behaviors green; Rust hash matches Python for same content. **Fresco integration-fixture re-lock DEFERRED to Corey** (needs `MILPA_INTEGRATION_TESTS=1`, no network in sandbox) — see PENDING USER ACTION above. Gates: `./dev-rust test --workspace` 441+64 pass; Python 2190 passed.
- [x] H3d — shared cross-impl corpus invariant fixtures (run REAL fetcher on BOTH impls via H-infra tier): `fixture-296/297` eol=crlf hash-identity (declared hash is the LF-tree hash, so any smudge diverges/fails), `fixture-298` symlink-escape→`EXTRACT-SYMLINK-ESCAPE` + `fixture-299` safe-symlink positive, `fixture-300` LFS-pointer→`FETCH-GIT-LFS-POINTER`, `fixture-301/302` cross-transport byte-identity (git object-store vs tarball, same tree → same `content_hash` `985b34e3…`). Both generators gained a `symlinks: {path: target}` map (byte-identical commits) — **H5 can reuse this for submodule repos**. Runners extended to handle `expected/error` for error-class git-protocol fixtures. Gates: Python 2197 passed; Rust 265 conformance pass / 0 regressions, `--workspace` all ok.
- [x] H5 — #140 submodule recursion. `materialize_git_tree` now reads `.gitmodules` (pure text parse, no eval), resolves relative URLs via git's `${remote%/*}` rule against `superproject_url`, recurses each mode-160000 gitlink. `GitReceipt.submodule_shas: dict[str, str]` (Python) / `Vec<(String,String)>` via `ProvenanceRecord::Git.submodule_shas` (Rust). Lockfile emits/parses `submodule "<path>" sha="<40hex>"` child nodes, path-sorted. `FETCH-GIT-SUBMODULE-FAILED` (with `submodule_path=`, `submodule_url=` context) in `spec/errors.md` + `errors.py` + Rust `all_codes()` — bijection green. Tests: Python `TestH5SubmoduleRecursion` / `TestH5RelativeSubmoduleUrl` / `TestH5SubmoduleFailedSlug` / `TestH5ParseGitmodules` + Rust `h5_a/b/d` tests; lockfile round-trip in `test_lockfile.py`. Gates: Python 2218 passed / 30 skipped; Rust all ok. **Integration fixture re-lock: DEFERRED to Corey** (same `MILPA_INTEGRATION_TESTS=1 uv run pytest tests/test_integration.py -v` action as H3c).

## Open forks (awaiting Corey)
- **None.** Scope greenlit 2026-06-26 (object-store rewrite). Round 2 added no forks — all findings carried confident recommendations and were resolved into the RFC. Heads-up only: review materially GREW the work (H3 → 4 sub-slices; H-infra is a heavy slice; new symlink-escape security work) — the loop will run longer than the round-1 slice count implied.

## Key decisions (round 2)
- **NEW security hole closed:** object-store symlink blobs (mode 120000) bypass `SafeExtractor` → escape vector (`../../etc/passwd`). H3 applies per-symlink lexical containment, reuses `EXTRACT-SYMLINK-ESCAPE`.
- **Hash-churn analysis was backwards:** corpus does NOT churn (`MockedGitFetcher` stages verbatim, no git); only the real-network fresco fixture churns. Corollary: H3's core invariant has ZERO corpus coverage → H-infra is load-bearing.
- **`materialize_git_tree` primitive:** single per-impl function = the enforcement chokepoint (blob writes, `--batch`, fixed mode, LFS detect, symlink-escape, submodule recursion). Makes 3 of 4 predicates structural (not prose-MUST). 4th (bounded) enforced at admission chokepoint.
- **`--no-checkout` clone** is mandatory — a default clone runs smudge filters and defeats the whole mechanism.
- **Submodule relative URLs NOT "absorbed":** H5 must read `.gitmodules`, resolve relative URLs vs superproject provenance URL.
- **H4 before H3** (dependency fix). **H3 split** into a/b/c/d.
- OCI carve-out from H1 streaming cap (oras bypasses milpa HTTP); bounded via digest-pin + admission stat.
- `cat-file --batch` (not per-blob: process-count DoS); empty dirs not synthesized (normative); fixed on-disk mode 0644/0755; LFS = first-line exact match + `path=` metadata.
- Slugs land WITH their implementing slice (bijection lint), not upfront.

## Notes for stage 4 (code-review)
- **H5 cross-impl convergence is via PARALLEL impl-side unit tests, not a SHARED conformance fixture.** Both impls have mirror tests (`TestH5*` in Python `test_git_fetcher.py`; `h5_*` + `make_superproject_with_submodule` in Rust `fetchers_tests.rs`, building a real mode-160000 gitlink via `git update-index --cacheinfo`). The RFC's literal ask was an H-infra shared superproject+submodule fixture asserting one golden hash on both runners; the generator was NOT extended to emit gitlinks. Defensible (schema-growth cost) but the strongest convergence proof (single shared golden hash for a submodule superproject) is absent — review should decide whether to require the shared fixture.
- Final gates run by the control loop (not just subagents): Python **2218 passed, 30 skipped**; Rust **735 passed, 0 failed** (`./dev-rust test --workspace`).
- Nothing committed — all work is in the tree awaiting review + commit.

## ✅ CLOSED OUT (2026-06-27)
- **Committed:** `7341d34` (73 files, +10884/−412; `closes #143/#144/#145/#149/#140/#175` on push — NOT yet pushed).
- **All findings resolved:** every Crit/High/Med (4 review rounds) + all Lows, except one Rust lzma/xz decompressor unify blocked by a lzma-rs HRTB limitation (documented in-code).
- **Follow-up filed:** #177 (conformance gap — realistic-size + hostile-tree-entry + submodule golden-hash fixtures; the gap that let the 2 Criticals escape the suite).
- **Final gates:** Python 2251 passed / 30 skipped; Rust 755 passed / 0 failed.
- **Fresco re-lock — N/A (phantom):** `tests/test_integration.py` was DELETED in the clean-room swap (`5ae87ad`); there is no pinned real-network fixture to re-lock. The handoff/CLAUDE.md "Real fresco verification" blurb is STALE (describes the retired 196-line suite). Instead, ran a live end-to-end smoke test (2026-06-27, real network) of the committed object-store path: temp project → `milpa fetch` real `intonaco` → **resolved 7 deps** (intonaco/chronos/results/stew/bearssl/httputils/unittest2), **`milpa verify` → verified 7 deps (exit 0)**, no `.git` leaked into `_deps/`. Rewrite validated against the real tree.
- **CLAUDE.md doc-staleness (flagged to Corey):** the "Real fresco verification" / "test_integration.py primary fixture" lines should be updated to reflect the H-infra tier (real git vs generated local repos, no network) that replaced the retired suite.
- **Optional:** `git push` (auto-closes the 6 referenced issues).

## Review ledger (stage 4) — ✅ FLOOR REACHED (0 Crit/High/Med)
- **Rounds:** R1 (find: 6 lenses + 7 verifiers → 23 findings) → R1 fixes → R2 re-review (found 9 new incl. 1 cross-impl High my own fix introduced) → R2/R3 fixes → R3 re-review (security CLEAN; found R3-01 High + R3-02 Med) → R4 fixes → R4 verify: **0 Crit/High/Med, only Lows remain.**
- **Final gates:** Python **2245 passed / 30 skipped**; Rust **753 passed / 0 failed**. Bijection green (reused slugs only). Spec updated to match impl (errors.md trigger; plugin-contract §2.3.5 depth-bound, §2.3.7 entry-path containment + non-UTF-8→ID-NON-UTF8-RELPATH, §2.4.4 decomp stream-cap + lzma attempt-decode; stale cross-refs fixed).
- **All Crit/High/Med (R1-01..R1-16, R2-01..R2-07, R1-16b, NEW-C, R3-01, R3-02) → RESOLVED + regression-tested.** Nothing committed (awaiting Corey's go-ahead).
- **Remaining Lows (deferred per mandate "leave Lows"):** R1-17 `LOCK_PROV_FIELD_ARITY` reuse for submodule-node errors · R1-18 Rust hardlink-read mis-slug EXTRACT-ZIP-SLIP · R1-19 Rust 32-bit `usize` overflow (unreachable on 64-bit) · R1-20 Python `batch_shas` dup SHAs (perf) · R1-22 `SIZE_EXCEEDED_PREFIX` string sentinel in `HttpGet` · R1-23 misc (lazy `import posixpath`, extract_tar double-buffer, identity.md §5 table) · R3-design: Rust `decompress_capped_lzma`/`_xz` structural dup, `run_git_plain`/`run_git_in_plain` shallow wrappers. → fold into a follow-up cleanup issue.


- **Stage:** 4 (code-review). Round 1: 6 lenses + 7 verifiers → 23 findings. Mandate (Corey, 2026-06-27): fix Crit+High+Med, leave Lows; §2.4.2 → implement stat in both impls. Round-1 fixes applied via 3 disjoint-file sonnet agents (2 Python + 1 Rust), TDD. **Gates after fixes: Python 2240 passed/30 skipped; Rust 750 passed/0 failed.** Spec docs updated (errors.md trigger note; plugin-contract §2.3.5 depth-bound, new §2.3.7 entry-path containment, new §2.4.4 decomp stream-cap + lzma-alone; stale cross-refs fixed). **Round 2 re-review in flight** (security/correctness, cross-impl convergence, design).

| id | sev | finding | status | proof / reason |
|----|-----|---------|--------|----------------|
| R1-01 | CRIT | Git blob/gitlink entry path has NO zip-slip containment in `materialize_git_tree` (both impls) — `dest_root/entry_path` with `../../` or `/abs` → arbitrary file write. Only symlink *targets* are checked. | fixed-R1 (re-review pending) | VERIFIED HOLDS (experimental: crafted tree via `git hash-object -t tree --literally`). py git.py:278/339, rs fetchers.rs:564/648 |
| R1-02 | CRIT | Rust `cat-file --batch` stdin written then stdout drained on ONE thread → deadlock at ~20–50 blobs (~64KiB stdout). Latent: fixtures too tiny to hit. Real dep would hang. | fixed-R1 (re-review pending) | VERIFIED HOLDS rs fetchers.rs:455-460; Python REFUTED (`communicate()`) |
| R1-03 | HIGH | Unbounded submodule recursion — no visited-set/depth-limit (both impls). Deep alternating chain → stack exhaustion + N clones (DoS). | fixed-R1 (re-review pending) | VERIFIED HOLDS py git.py:343, rs fetchers.rs:653 |
| R1-04 | HIGH | H5 `submodule_shas` dead on WRITE path (both impls): resolver never reads `receipt.submodule_shas` → lockfile records zero submodule provenance; parse/format code dead on write. | fixed-R1 (re-review pending) | VERIFIED HOLDS py git.py:115/resolver.py:3277, rs fetchers.rs:987/resolver.rs:3635 |
| R1-05 | HIGH | Rust submodule closure ignores pinned `_sub_sha` (no presence check) → non-tip gitlink yields `FETCH-GIT-FAILED` vs Python `FETCH-GIT-SUBMODULE-FAILED` (slug divergence + possible wrong/failed materialize). | fixed-R1 (re-review pending) | VERIFIED HOLDS rs fetchers.rs:954 vs py git.py:665-673 |
| R1-06 | HIGH | Python lzma-alone (`.tar.lzma` FORMAT_ALONE, magic `5d 00`) bypasses Layer-1 `_decompress_capped` → `r:*` hands it to tarfile's uncapped internal decompressor → OOM bomb. Rust unaffected. | fixed-R1 (re-review pending) | VERIFIED HOLDS safe_extract.py:182/257/293 |
| R1-07 | HIGH (SPEC ESCALATION) | plugin-contract.md §2.4.2 NORMATIVE "CasAdmittingFetcher MUST stat staged tree → raise FETCH-DOWNLOAD-SIZE-EXCEEDED" unimplemented in BOTH impls. Spec-vs-impl divergence → Corey decides: implement or amend spec. | fixed-R1 (re-review pending) | VERIFIED (gap real) cas_admitting.py:107-114, rs fetchers.rs:1596-1618 |
| R1-08 | MED | Decomp-cap off-by-one cross-impl divergence: Python admits exactly `decomp_cap`, both Rust paths reject. Determinism break at `(1<<30)+512`. | fixed-R1 (re-review pending) | VERIFIED HOLDS safe_extract.py:185 vs fetchers.rs:78/1279 |
| R1-09 | MED | Python `_submodule_fetch` `git fetch origin <sha>` returncode unchecked → silent miss → wrong slug (sibling of R1-05). | fixed-R1 (re-review pending) | py git.py:671-675 |
| R1-10 | MED | Rust submodule scratch dir `sub_<subsec_nanos>` collision → spurious FAILED or silent wrong-content. | fixed-R1 (re-review pending) | rs fetchers.rs:957 |
| R1-11 | MED | No shared conformance fixture for submodule cross-impl hash convergence — exactly why R1-04/05 divergences went uncaught. | fixed-R1 (re-review pending) | handoff-flagged; corpus gap |
| R1-12 | MED | Rust `decomp_cap` formula duplicated (fetchers.rs:1404,1519); Rust `Limits` lacks `decomp_cap` field (asymmetry w/ Python). | fixed-R1 (re-review pending) | design |
| R1-13 | MED | Python `proc.stderr` FD leak in tarball `_curl` (every invocation, all exit paths). | fixed-R1 (re-review pending) | tarball.py:148-196 |
| R1-14 | MED | Rust `ensure_commit_present` step-2 returns Ok without re-verifying commit present after `git fetch` success → may skip steps 3-4, wrong tree. | fixed-R1 (re-review pending) | rs fetchers.rs:887 |
| R1-15 | MED | ls-tree path C-quoting/non-UTF-8 not handled (`-z` not used) → mangled paths; Rust lossy-converts then writes (compounds R1-01). | fixed-R1 (re-review pending) | py git.py:199-208, rs fetchers.rs:405 |
| R1-16 | MED | Submodule relative-URL `//` normalization diverges (py posixpath.normpath vs rs split/filter). | fixed-R1 (re-review pending) | py git.py:485, rs fetchers.rs:786 |
| R1-17 | LOW | `LOCK_PROV_FIELD_ARITY` slug reused for structurally distinct submodule-node errors. | fixed-closeout | lockfile.py:+773 |
| R1-18 | LOW | Rust hardlink read failure mis-slugged `EXTRACT-ZIP-SLIP` (should be IO error). | fixed-closeout | safe_extract.rs:253-263 |
| R1-19 | LOW | Rust framed-stream/USTAR size parse `usize` overflow on 32-bit targets (not reachable on 64-bit). | fixed-closeout | fetchers.rs:520, safe_extract.rs:478 |
| R1-20 | LOW | Python `batch_shas` duplicate SHAs inflate cat-file payload (compounds R1-02). | fixed-closeout | git.py:220 |
| R1-21 | LOW | Stale spec cross-refs: plugin-contract §2.3.5 → lockfile-schema "§7" (nim.cfg) should be §4.1; §2.3.2/§2.4.2 forward-ref notes now stale; RFC slice DONE markers missing. | fixed-R1 (re-review pending) | doc hygiene |
| R1-22 | LOW | `SIZE_EXCEEDED_PREFIX` string sentinel embedded in `HttpGet` Err(String) — leaky implicit contract. | fixed-closeout | design fetchers.rs |
| R1-23 | LOW | Misc: lazy `import posixpath` (git.py:454); double-buffer extract_tar memory (safe_extract.py); identity.md §5 table incomplete (pre-existing). | fixed-closeout | low polish |

### Round 2 (re-review of round-1 fixes) → round-3 fixes IN FLIGHT (agents a11585535560e6487 Python, ad500d33193872c36 Rust)
3 lenses + targeted verification. Round-1 fixes CONFIRMED-FIXED: R1-01, R1-04 (lockfile submodule nodes converge), R1-05/09, R1-07, R1-08 (all 3 codec paths). New findings (mandate Crit+High+Med, AUTONOMOUS — no fork; NEW-C resolved via existing ID-NON-UTF8-RELPATH project decision):

| id | sev | finding | status |
|----|-----|---------|--------|
| R2-01 | HIGH | Rust submodule visited-set GLOBAL not path-local → falsely rejects diamond/shared-submodule; violates §2.3.5 "current recursion path". Fix: clone-per-child (match Python). | fixed-R3 (re-review in flight) |
| R2-02/NEW-D | HIGH | lzma-alone guard incomplete: Python `5d 00` magic misses non-0x5d properties byte (bypass); Rust lacks dispatch. Fix: magic-independent attempt-decode FORMAT_ALONE under cap, both impls. | fixed-R3 (re-review in flight) |
| NEW-C | HIGH | Non-UTF-8 ls-tree path: Python latin-1 vs Rust lossy → silent content_hash divergence. Fix: both raise ID-NON-UTF8-RELPATH. | fixed-R3 (re-review in flight) |
| R2-03 | MED | Rust cat-file writer-thread join swallows panic + misleading comment. | fixed-R3 (re-review in flight) |
| R2-04 | MED | Rust vestigial dead no-op block before real cat-file status check. | fixed-R3 (re-review in flight) |
| R2-05 | MED | Python tarball.py `with proc:` + manual close/wait → confused double-drain. | fixed-R3 (re-review in flight) |
| R2-06 | MED | Rust DECOMP_CAP_OVERHEAD defined twice. | fixed-R3 (re-review in flight) |
| R2-07 | MED | Two shape-asserting tests fragile (`_GIT_TRANSPORT_FLAGS==[]`, depth==16 literal). | fixed-R3 (re-review in flight) |
| R1-16b | MED | R1-16 not converged: Python posixpath.normpath preserves `//`; fix Python to explicit-collapse. | fixed-R3 (re-review in flight) |
| R2-L | LOW(folded) | dead run_git* (Rust), `_MAGIC_ZIP` local (Python), transport_to_record doc note. | fixed-R3 (re-review in flight) |

**Round-3 fixes DONE** (agents: Python a11585535560e6487 → 2244 passed; Rust ad500d33193872c36 → 753 passed, 0 fail). Spec edits DONE: §2.3.7 non-UTF-8 → ID-NON-UTF8-RELPATH; §2.4.4 lzma-alone → attempt-decode-under-cap (no reliable magic). **Round-3 re-review DONE.** Security: CLEAN (no exploitable issues; DoS fix holds, lzma decode bounded in+out, non-UTF-8 raises pre-write). Convergence: items 2/3/4 CONVERGED; design: no High. Two residuals → round-4 micro-fixes IN FLIGHT (Python aa1ddcb7dcbf43d88 / Rust a937bf64a3ab69281):
- **R3-01 (HIGH)** Python `_decompress_capped` left an UNGUARDED `5d 00` lzma fast-path → uncaught LZMAError on malformed `5d 00`-prefixed stream while Rust falls through to plain-tar (divergence). Fix: delete fast-path → single guarded structural-probe path (matches Rust); drop unused `_MAGIC_LZMA_ALONE`; fix 0xE0→0xD8 props bound.
- **R3-02 (MED)** Rust lzma-bomb test's first assertion tests Layer-2 cap while claiming decompressor cap (misleading comment + integration gap). Fix: honest assertions + decompressor-cap coverage.
Lows deferred (R3 design): Python/Rust lzma path duplication, run_git_plain shallow wrappers, decompress_capped_lzma/xz dup — all Low, mandate=leave.
Floor = a round with 0 Crit/High/Med.

**Refuted / dropped (recorded, not presented as findings):** Rust "ls-tree emits tree objects written as files" (ls-tree -r doesn't emit intermediate trees — Low residual only); Python cat-file deadlock (REFUTED — `communicate()` is concurrent); §2.4.2 "literally as stated" (refuted wording, but underlying gap kept as R1-07).
**Confirmed clean (security):** no command injection (argv everywhere, `--end-of-options` placed correctly); symlink-target escape guarded; hardlink zip-slip two-pass guarded; LFS first-line detection robust; OCI arg-injection guarded; single cat-file process (not per-blob).
