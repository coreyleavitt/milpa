# Native OCI pull — handoff

- **Stage:** 3 tdd — **COMPLETE. All 12 slices (S0–S11) DONE + verified.** Loop stopped. Next: stage 4 `/code-review docs/rfc-native-oci-fetch.md` (2-3 rounds, ship when a round finds 0 critical/high).
- **⚠ WORK IS UNCOMMITTED** — the whole grind (both impls + spec + `conformance/oci-transport/` fixtures + this RFC/handoff + attestation-v1 docs from before) is unstaged. Corey has NOT been asked; commit only on request (CLAUDE.md: direct-to-main pre-1.0, `closes #N`, no co-author trailers). Suggested commit grouping when asked: one `feat(oci): native in-process OCI pull — remove consumer oras/curl shell-outs` (impls + fixtures) or split spec/impl.
- **MANUAL CHECKPOINT for Corey (not a gate):** live `ghcr.io/coreyleavitt/softlink@sha256:…` pull + re-run the `goodtogo` (softlink+proptest) project that failed on absent `oras`; confirm `content_hash` matches. This env has no outbound network, so it was never run here.
- **S10 verified:** §2.4.2 oras exemption struck; registry-protocol.md §7 normative pull-side (token→manifest→blob, digest-verify, manifest-list reject, cap); TNG rationale rewritten; publishing.py `_extract_layer_digest` tightened (uses PUBLISH-MANIFEST-FETCH-FAILED, +3 tests); errors.md Triggered lines reframed; NO slug drift (both bijections green); conformance-fixtures.md zero diff. Full pytest exit 0; Rust conformance green.
- **⚠ On resume — VERIFY S9 DIRECTLY** (agent lines mangled): Python `_process_oci_worker` (resolver.py:4603) `except MilpaError` now RE-RAISES unchanged (not FETCH-ALL-FAILED); Rust `process_oci` (resolver.rs:3448) mirrors. Test: OCI fetch raising FETCH-OCI-DIGEST-MISMATCH surfaces that slug (NOT FETCH-ALL-FAILED), phase= survives. `cd impls/python && uv run pytest -q` exit 0; `./dev-rust test -p milpa-core` green. General git-mirror FETCH-ALL-FAILED path (resolver.py ~4463-4500 `_fetch_any`, resolver.rs ~3823) must stay UNTOUCHED. Then S10, S11.
- **S10 PLAN (spec+docs, spec-edit bar):** (a) normative pull-side manifest-shape section in `spec/registry-protocol.md` naming token/manifest/blob as the normative phase decomposition; (b) STRIKE the `spec/plugin-contract.md §2.4.2` oras cap-exemption (lines ~401-406) + require fixed cap on OCI blob + small cap on token/manifest; (c) rewrite `TNG-BAD-OCI-DIGEST`/`TNG-UNSAFE-OCI-FIELD` rationale (threat now malformed URL/header, not oras argv); (d) tighten publish `_extract_layer_digest` in `impls/python/milpa/publishing.py` to reject anything but the single-milpa-layer shape; (e) fix workflow/spec comments asserting consumer shells to curl/oras; update `spec/errors.md` FETCH-OCI-PULL-FAILED entry ('oras pull exited non-zero' → native phase). `conformance-fixtures.md §2.3.5` deliberately UNCHANGED. Also update `errors.md` FETCH-OCI-NO-TARBALL/AMBIGUOUS-TARBALL '**Triggered:**' lines (mention OciFetcher/oras → select_source_layer). Both impls' bijection stays green (no new slugs).
- **S11 PLAN (defer-issue):** `gh issue create` for private-registry creds + registry diversity (Docker Hub `auth.docker.io`, ECR/`public.ecr.aws`, self-hosted plain-HTTP, custom-CA/proxy). File it NOW (defer-file-now). Then the grind is DONE — stop the loop (PushNotification + ScheduleWakeup stop).
- **S7/S8 SPLIT DECISION (resolved by control loop, goal-determined — mirrors Python S5/S6):** S7 = build the Rust OCI client as a NEW `milpa-core/src/oci_client.rs` module driving the SAME `conformance/oci-transport/*.json` fixtures, does NOT touch `fetchers.rs::fetch_oci` (oras stays). S8 = retrofit `fetch_oci` (fetchers.rs:1855; delete `Command::new("oras")` @1890, keep extract tail) to compose the native client as default. Rust `Provenance::Oci{registry,repository,digest,source}` (milpa-types/src/lib.rs:373).
- **⚠ On resume — LAUNCH S6** (Python OCI swap, delegate to sonnet, NO-GIT): rewrite `milpa/fetchers/oci.py::OciFetcher.fetch` to compose `oci_client.OciRegistryClient` + `select_source_layer` directly (token→manifest→select→blob→safe_extract); **DELETE `OciPull`, `make_oras_pull`, `make_native_oci_pull` (if present), and the redundant filename-suffix `.tar.gz`/`.tgz` scan** (oci.py ~288-306). The OciFetcher must build a default `OciRegistryClient(bounded_http.request, TokenCache())`. Keep the injected-transport seam for tests (drive via the same conformance/oci-transport fixtures / existing MILPA_MOCKED_FETCHES whole-fetcher mock for identity). Gate: `cd impls/python && uv run pytest -q` exit 0. Then S6's manual real-network smoke is a CoREY checkpoint (NOT a slice gate) — note it, don't block on it. VERIFY S6 directly (agent lines get mangled): grep shows OciPull/make_oras_pull GONE; OciFetcher uses oci_client; pytest green.
- **⚠ On resume / post-compaction — S4 STATUS (code landed, 1 flaky milpa-cli test to fix):**
  - VERIFIED GOOD: `grep 'Command::new("curl")'` in `impls/rust/crates/*/src` = EMPTY (zero production curl); `bounded_http` adopted in fetchers.rs/dep_decl_store.rs/entry_bundle_store.rs/main.rs; milpa-core tests exit 0; `build --workspace` OK. Bundle-404 second-request correctly dropped + documented (main.rs `build_bundle_http_fn` ~3803, reads `resp.status==404`).
  - `require_attested_entries` unused-var warning (main.rs `maybe_index` ~3434) is **PRE-EXISTING** (present in HEAD baseline `git show HEAD:…`), NOT an S4 regression — leave it, out of scope.
  - **FIX APPLIED (awaiting stress-verify):** bounded_http.rs now uses a process-wide cached `Agent` via `OnceLock` (`shared_agent()`), replacing per-call `build_agent()` in `request()`. Reads proxy-env ONCE (idiomatic ureq + kills the per-call data race). bounded_http's own 19 tests green. Stress-run (milpa-cli bin ×3) in bg `blqyt55k9` — if all 3 report "166 passed; 0 failed", S4 is done. If flake recurs, the residual is the single first-touch env read; escalate (test-harness warm-up) rather than re-patch.
  - **THE BUG (now fixed, verifying): `./dev-rust test -p milpa-cli` was FLAKY — 1 test intermittently failed** (failed run 1, passed the grep run). ROOT-CAUSE DIAGNOSIS (strong, not yet 100% confirmed against the named test): `bounded_http.rs::build_agent()` is built **fresh per call** (see its doc comment ~L174-181) and ureq's default `Config` proxy = `Proxy::try_from_env()` → **reads `HTTP_PROXY`/`NO_PROXY` from process env on EVERY request**. milpa-cli tests do `unsafe { set_var/remove_var }` (serialized only among themselves via `ENV_MUTEX`). The per-call env READ is a cross-thread data race with those writes ⇒ intermittent failure (flake ⟺ race). PLANNED ROOT-CAUSE FIX: read proxy-env ONCE via `OnceLock` (or read once in build_agent guarded), eliminating the per-call env read — still §0.1-compliant (proxy env is process-scoped). NOTE the doc comment deliberately avoided OnceLock for "live per-call detection" — that rationale is wrong (created a data race for no real benefit; proxy env doesn't change mid-process).
  - GOTCHA: `./dev-rust test … | tail`/`| grep` MASKS exit code (pipe → tail's 0). Always read the "test result:" line.
  - RESUME STEPS: (1) get failing test NAME (full-capture run was `$CLAUDE_JOB_DIR/tmp/s4_milpacli_full.txt`; grep `failures:`/`FAILED`) or run `RUST_TEST_THREADS=1` to see if serialization hides it (confirms race); (2) confirm it makes a bounded_http request + races env; (3) apply the OnceLock proxy fix in bounded_http.rs; (4) reproduce-stress the fixed test (loop / filter) then whole milpa-cli green; (5) mark S4 done, launch S5. Do NOT trust idle S4-agent (a3580e385ec67034c) re-notifications (mangled).
- **Resume:** `/loop implement the next unimplemented RFC slice with /tdd, following the standing rules; after each slice report one progress line; stop when every slice is implemented`
- **Scope:** transport-unification — kill oras AND curl; one native in-process transport (ureq+rustls Rust / urllib Python). All consumer fetches (index/bundle/dep-decl/entry-bundle/tarball/OCI).

## Slices (RFC §5 canonical — one impl per slice, primitive-first)
- [x] S0 — spec-first + fixture infra. DONE: `FETCH-OCI-DIGEST-MISMATCH` in spec/errors.md + errors.py + fetch.rs (bijection RED→GREEN both impls). F1/F2 + §0.1 + redirect-strip NORMATIVE + unit-tier-only limitation → `spec/plugin-contract.md §2.4.5`. `conformance/oci-transport/{schema.json,README.md}` pinned (nested `response{}`, `expect_request_headers` w/ null=absent for redirect-strip). Isolation guard test added (oci-transport never enters corpus). Full pytest green; Rust bijection green.
- [x] S1 — Python `bounded_http` primitive. DONE: `impls/python/milpa/bounded_http.py` — `request(method,url,*,headers,cap,sink,timeout)->HttpResponse` (frozen dataclass, case-insensitive headers); pure `_origin`/`_same_origin` helpers; `_AuthStrippingRedirectHandler` (strips Authorization unless `(scheme,host,port)` matches); `_build_opener` retains ProxyHandler; `_stream_capped` fires FETCH-DOWNLOAD-SIZE-EXCEEDED mid-stream; transport errors→FETCH-DOWNLOAD-FAILED; HTTP status returned as data (401/404/307 not raised). 11 unit tests green (`tests/test_bounded_http.py`). Single `timeout=300s` (urllib's one knob). No caller migrated (that's S3). NOTE for verification: subagent's final report line was mangled — S1 was verified directly by the control loop (files+tests), not trusted from the agent summary.
- [x] S2 — Rust `bounded_http` primitive. DONE: `milpa-core/src/bounded_http.rs` — `request(method,url,headers,cap,Sink)->Result<HttpResponse,FetchError>` + pure `same_origin`; `pub mod` in lib.rs (milpa-cli can consume in S4). Deps: `ureq=3` (`rustls-no-provider` — NOT the umbrella feature, which pulls ring), `rustls=0.23`(`aws_lc_rs`), `rustls-native-certs=0.8`, `url=2.5`(already transitive). ureq auto-redirect disabled (`max_redirects(0)`)→own redirect loop for `(scheme,host,port)` auth-strip; `http_status_as_error(false)`. Timeouts 30s/300s. **VERIFIED by control loop:** 19 tests pass; `tree -i ring`=empty (no 2nd crypto backend); `tree -i aws-lc-sys`=single unified node. milpa-core whole-crate 1175 passed (agent-reported). No caller migrated (S4).
- [x] S3 — migrate Python unauth GET callers onto S1. DONE + VERIFIED: tarball (curl→`bounded_http.request` Path sink, MAX_COMPRESSED_BYTES) + index_cache `urllib_http_get`/`urllib_bundle_http_get` + dep_decl_store + entry_bundle_store all on `bounded_http.request`; zero residual urlopen/curl in production (only in comments). Caps added: `_INDEX_MAX_BYTES`=32MiB, `_INDEX_BUNDLE_MAX_BYTES`=4MiB, `_DEP_DECL_MAX_ARTIFACT_BYTES`=1MiB, `_ENTRY_BUNDLE_MAX_ARTIFACT_BYTES`=4MiB (distinct per-artifact, correctly not over-unified). file:// path handles status=None. Cap characterization tests added (test_dep_decl_store size-cap). Full pytest exit 0.
- [x] S4 — migrate Rust unauth GET callers onto S2. DONE + VERIFIED: all 8 curl sites → `bounded_http::request`; zero production curl (`grep 'Command::new("curl")'`=empty); one ureq backend for milpa-cli+milpa-core; bundle-404 second-request dropped (reads `resp.status==404`, documented `build_bundle_http_fn`). **Fixed a flaky proxy-env data race** discovered during verify: `bounded_http.rs` now caches one `Agent` via `OnceLock` (`shared_agent()`) — reads proxy-env once instead of per-call (was racing test `unsafe set_var`). VERIFIED: milpa-cli bin 166 passed ×3 stress runs; milpa-core 1175 passed; workspace build OK. Pre-existing unused `require_attested_entries` warning left as-is (baseline, out of scope).
- [x] S5 — Python OCI client. DONE + VERIFIED: `milpa/fetchers/oci_client.py` — `Layer`/`Manifest` frozen dataclasses; `tokenize_www_authenticate`+`select_bearer_challenge` (pure RFC-7235, fail-closed); `TokenCache` (striped per-(registry,scope), expires_in, invalidate/401-refetch-once); `OciRegistryClient(http,token_cache)` .token/.manifest/.blob (blob+manifest verify sha256 internally → FETCH-OCI-DIGEST-MISMATCH); `select_source_layer` pure gate; `OciHttpTransport`=structural Protocol over bounded_http.request (no adapter class). 46 tests green (test_oci_client.py); redirect test verified non-vacuous; stampede test deterministic. 12 real-digest fixtures under conformance/oci-transport/ conform to S0 schema (NO schema extension). Old oci.py/oras path UNTOUCHED (not default — S6). Full pytest exit 0 (confirming in bg b0ohhbg5k).
- [x] S6 — Python OCI swap. DONE + VERIFIED: `OciFetcher.fetch` composes `token→manifest→select_source_layer→blob→extract_tar` directly; DELETED `OciPull`, `make_oras_pull`, oras/subprocess machinery, and the `.tar.gz`/`.tgz` filename scan (only accurate comments about old argv threat remain). **Identity-correctness catch:** blob staged to a scratch tempdir (NOT literally `dest/source.tar.gz`) so the raw tarball never enters the CAS-hashed tree. Test callers migrated to `tests/_oci_fake_client.py::FakeOciClient`; obsolete oras/filename-scan tests deleted (covered by test_oci_client.py); publishing.py stale docstrings fixed. fixture-450 OCI identity passes. Full pytest exit 0. Manual softlink smoke = Corey checkpoint (pending).
- [x] S7 — Rust OCI client (NEW module only; retrofit is S8). DONE + VERIFIED: `milpa-core/src/oci_client.rs` (+oci_client_tests.rs, `pub mod` in lib.rs) — `trait OciTransport`+`BoundedHttpTransport`; `Layer`/`Manifest`; `tokenize_www_authenticate`+`select_bearer_challenge` (pure, hand-rolled, no regex dep); `select_source_layer`; `TokenCache` (striped, injectable clock); `OciRegistryClient` .token/.manifest/.blob (explicit `scheme`, verifies sha256 internally). `phase=` as message prefix (`FetchError::Transport` has no context map), asserted via `.message().contains("phase=token")`. 33 tests green; drives all 12 S5 fixtures UNMODIFIED; `FixtureTransport` reuses real `bounded_http::same_origin`. milpa-core whole-crate 1208 passed (agent-reported). fetch_oci still on oras (S8).
- [x] S8 — Rust OCI swap. DONE + VERIFIED: `fetch_oci` (fetchers.rs:1875) composes `OciRegistryClient::new(BoundedHttpTransport, TokenCache)` + `select_source_layer` + injectable `fetch_oci_with_client` seam + `OCI_SCHEME` const (not hardcoded https); zero `Command::new("oras")` in production Rust; extract tail kept. milpa-core 1209 passed, milpa-conformance green (OCI identity fixtures pass). Manual softlink smoke = Corey checkpoint (pending).
- [x] S9 — #198 OCI call-site (both impls). DONE + VERIFIED: Python `_process_oci_worker` `except MilpaError: raise` (bare re-raise, definitive OCI slug + phase= reaches CLI); Rust `process_oci` already propagated raw FetchError (no wrap existed) — regression tests added both impls (`test_oci_worker_error_propagation.py`; `FailingOciReg`+2 tests in resolver_tests.rs). General git-mirror FETCH-ALL-FAILED path (`_fetch_any`, resolver.rs:3823) untouched. No fixtures changed (all FETCH-ALL-FAILED fixtures are tarball). Full pytest exit 0; milpa-core 1211 passed; milpa-conformance green.
- [x] S10 — spec+docs. DONE + VERIFIED: registry-protocol.md §7 normative pull-side (token→manifest→blob phases NORMATIVE, media types, digest-verify-before-parse, manifest-list reject, single-milpa-layer gate, fixed cap); struck plugin-contract §2.4.2 oras exemption (cap now applies to OCI blob + small cap token/manifest); rewrote TNG-BAD-OCI-DIGEST/TNG-UNSAFE-OCI-FIELD rationale (malformed-value-to-native-client, not argv); tightened publishing.py `_extract_layer_digest` (single-milpa-layer, slug PUBLISH-MANIFEST-FETCH-FAILED, +3 tests, fixed 2 stale fixtures); errors.md FETCH-OCI-* Triggered lines reframed; fixed cli-contract.md stale 'Rust curl' claim. NO slug drift; conformance-fixtures.md zero diff. Full pytest exit 0; Rust bijection+conformance green.
- [x] S11 (deferred) — private creds + registry diversity. DONE: GH issue **#200** filed (https://github.com/coreyleavitt/milpa/issues/200) — Docker Hub auth.docker.io, ECR/public.ecr.aws, self-hosted plain-HTTP, custom-CA/proxy, private-registry credential carry. Defer-file-now satisfied.

## Open forks (awaiting Corey)
- NONE. Both architect rounds complete; all fixes applied. Round-2 sub-decisions (§0.1: proxy-env, rustls-native-certs, aws-lc-rs, timeouts, no-rollback) were goal-determined under milpa non-negotiables, applied directly.

## Key decisions (round 2)
- **OciPull closure DELETED** (was a redundant 4th layer duplicating the one-tarball gate at oci.py:288-306). OciFetcher composes OciRegistryClient + select_source_layer (pure fn) directly. blob() verifies sha256 INTERNALLY = sole digest site.
- **bounded_http shape pinned**: `request(method,url,*,headers,cap,sink)->HttpResponse`; sink=BytesIO|Path (4 GiB cap forbids always-buffer-to-bytes). OciHttpTransport = alias, not a distinct type.
- **8 Rust curl sites** confirmed (grep): 5 main.rs + dep_decl_store + entry_bundle_store + fetchers.rs. dep_decl/entry_bundle have NO seam → seam-first.
- **Slice re-numbered + split by impl** (no single RED-GREEN across pytest+cargo): S0-S11, primitive-first. §2-§4 prose numbering reconciled to §5.
- **Transport parity sub-decisions** (§0.1): proxy-env honored both impls; rustls-native-certs (OS trust, curl-parity, S11-additive); aws-lc-rs (match sigstore, avoid double crypto backend); conservative timeouts (in-process no-timeout hangs a worker forever); no rollback shim (no-legacy-pre-v1).
- **Token cache** = field on client (one per resolve), per-key striped locking, expiry + 401-refresh.
- **Redirect predicate** = origin (scheme,host,port), not host-alone (catches same-host scheme-downgrade). ghcr CDN is presigned → strip is safe.
- **WWW-Authenticate** = specified tokenizer, fail-closed, non-Bearer→clean unsupported-auth error.
- **Real-network smoke** = manual Corey-run checkpoint (like "Real fresco verification"), NOT a slice gate (slices hermetic).
- **Canned-transport fixture** pinned at `conformance/oci-transport/` (outside spec-v1), schema fixed in S0.
- **Git fetcher** explicitly out of scope (wire protocol ≠ bounded GET).

## Review ledger (stage 4)

Round 1: 6 reviewers (py-correctness, rust-correctness, security, design, cross-impl, spec). All findings adversarially verified (direct read / live PoC). Awaiting Corey's fix-mandate approval.

| id | sev | finding | status | proof / reason |
|----|-----|---------|--------|----------------|
| C1 | CRIT | Python `bounded_http` has no scheme constraint; attacker-controlled OCI `realm` (WWW-Authenticate) routed through it → SSRF (http/ftp) + local file-read + cred-exfil chain | fixed | py: explicit opener handler list (drops FTP/Data, keeps File+ProxyHandler+HTTPDefaultError+Unknown), realm scheme∈{http,https} guard in _acquire_token. rust: realm-scheme guard in acquire_token + defense-in-depth scheme guard in bounded_http::request (ureq inert anyway). +fixture token-realm-non-http.json both impls. RED confirmed (read /etc/passwd pre-fix). Py 3916 pass / Rust 1215 + conformance green |
| H1 | HIGH | manifest valid-JSON-but-not-object: Python uncaught TypeError→MILPA-INTERNAL (bypasses #198 except MilpaError); Rust wrong slug NO-TARBALL not PULL-FAILED phase=manifest | fixed | is-object guard oci_client.py:697 / oci_client.rs:803 → FETCH-OCI-PULL-FAILED phase=manifest; +fixture manifest-not-an-object.json both impls. Gate pending |
| H2 | HIGH | redirect-loop-exceeded fail-open in Python (no own hop cap; urllib HTTPError treated as data → returns 307) vs fail-closed Rust (MAX_REDIRECT_HOPS) | fixed | py _MAX_REDIRECT_HOPS=10 pinned + fail-closed FETCH-DOWNLOAD-FAILED; test_redirect_loop_exceeding_hop_cap_raises_download_failed. Gate pending |
| H3 | HIGH | tarball body fully buffered (py BytesIO / rust Vec) at 4GiB cap → N-worker N×4GiB DoS; contradicts RFC §3.3 "(and tarball body) pass a file Path" | fixed | HttpGet seam bytes→Path both impls; streams download to scratch file, hashes streaming, extract from Path (py) / read-back parity w/ OCI blob (rust). Identity unchanged. Residual: Rust &[u8] extract read-back = pre-existing shared limitation → filed #202. Gate pending |
| M1 | MED | unguarded `self._http` in _acquire_token/_get_with_auth_retry: connection-level failure leaks bare FETCH-DOWNLOAD-FAILED, no phase=/registry= (violates §3.4) | fixed | try/except MilpaError → FETCH-OCI-PULL-FAILED phase= wrapping all token/manifest/blob transport calls oci_client.py. Gate pending |
| M2 | MED | redirect auth-strip basis: Rust per-ORIGINAL-origin (can restore auth on hop back), Python monotonic | fixed | standardized MONOTONIC both impls (rust `stripped` latch bounded_http.rs:360); §3.8 amended "strip on first cross-origin hop, never restore"; 3-hop return-to-origin tests both. Gate pending |
| M3 | MED | connect-timeout: Python single 300s knob vs Rust 30/300 split (RFC §0.1) | fixed | §0.1 amended to document Python single-knob stdlib limitation (accepted deviation; read/connect bounded) + code comment. No behavior change |
| M4 | MED | access_token fallback + token-phase malformed-JSON/missing-fields: ZERO test coverage both impls (§7.3 MUST) | fixed | +fixtures token-access-token-only.json, token-neither-field.json + token-invalid-json coverage both impls. Gate pending |
| M5 | MED | Rust `FetchError::Transport(code,msg)` no context slot → phase= is msg string-prefix, tests `.contains("phase=")` = the "stringly not structured" §3.4 rejects (py uses .context["phase"]). Precedent: error.rs RatchetViolation{...,digest} | fixed | new dedicated `FetchError::OciTransport{code,message,phase:&'static str}` + phase() accessor; oci_client.rs sets phase structurally; tests assert .phase()==Some("token"). RED-verified. resolver.rs:6309 +1 match arm (unavoidable, anticipated). No new slug. milpa-core 1231 green |
| M6 | MED | Rust `trait OciTransport`+`struct BoundedHttpTransport` ceremony vs py zero-adapter Protocol + vs Rust's own `HttpGet=Box<dyn Fn>` idiom | fixed | replaced w/ `pub type OciTransport = Box<dyn Fn(...)+Send+Sync>` (mirrors HttpGet); fetch_oci passes Box::new(bounded_http::request). Green |
| M7 | MED | Rust fetch_oci scratch dir predictable `.{name}.oci-pull` + remove→create TOCTOU; local attacker symlink-redirects blob write (no O_NOFOLLOW). Python uses tempfile.TemporaryDirectory | fixed | tempfile crate (dev→prod dep) for BOTH oci-pull AND the new tarball-pull scratch (H3 added a 2nd predictable path); Drop-cleaned. RED-verified mid-flight. No cross-device rename (reads-to-mem+extracts to dest) |
| M8 | MED | Rust OCI fixture replay (FixtureTransport) reimplements redirect/cap, never calls real bounded_http::request → H2/M2-class divergences can't surface Rust-side | fixed | added real-transport end-to-end test oci_registry_client_real_transport_happy_pull_end_to_end (Box::new(bounded_http::request) vs local server, real sha256 verify). 13-fixture rewrite assessed disproportionate (bounded_http now has own real-TCP tests) |
| M9 | MED | HttpDepDeclStore/HttpEntryBundleStore acknowledged duplicate code path (CLAUDE.md "dupes are bugs"); docstring promises P4 revisit, no issue filed (defer=file-now) | fixed | filed #201 (extract generic ContentAddressedHttpArtifactStore at P4); resolution = file-now per defer discipline |
| L1 | LOW | Rust pull_and_extract_oci no remove_dir_all(dest) on extract_tar fail (asymmetric w/ fetch_tarball); partial tree on hostile tar. Pre-existing | open | fetchers.rs:1963 |
| L2 | LOW | Python _get_with_auth_retry drops scheme on 401-retry token refetch (always https); latent | open | oci_client.py:534 |
| L3 | LOW | Rust blob() writes Sink::File before status check; safe today (scratch) but implicit reusable-API contract | fixed | blob() documents scratch-path contract + remove_file(dest) on non-200 so error body never seen as a layer; +test |

### Round 2 (re-review of the round-1 fixes) — new findings, all fixed

Corey directive mid-round: "stop deferring stuff" ([[feedback_no_deferring_in_loop]]) — pre-v1, fix full scope, don't file-and-punt fixable findings. So issues #201/#202/#203 (originally filed as defers) are being FIXED now and closed; Lows L1/L2/L3 folded in.

| id | sev | finding | status | proof / reason |
|----|-----|---------|--------|----------------|
| Bug1 | HIGH | Python bounded_http.request wraps only connect phase; mid-stream RST escapes unwrapped → OCI hits resolver generic except → reopens #198/S9 FETCH-ALL-FAILED | fixed | py: streaming block now in try/except→FETCH-DOWNLOAD-FAILED (SIZE-EXCEEDED still propagates). rust: ureq/? already maps mid-stream io err; +confirming test. Py 3938 pass |
| Bug2 | MED | no Content-Length completeness check; clean-truncated body = silent 200 → OCI reads as DIGEST-MISMATCH (tampering), tarball TOFU as EXTRACT-FAILED | fixed | py _reject_if_truncated scoped to declared<=cap (preserves lying-CL>cap characterization tests). rust: ureq enforces CL completeness natively; +tests. RESIDUAL divergence (CL>cap+complete body: py accepts, rust rejects) — surface to Corey |
| SecF2 | LOW | Rust bounded_http scheme guard checked only on initial url, not per redirect hop (backstopped only by ureq) | fixed | ensure_http_scheme now per-hop vs current_url; RED-verified redirect-to-file/ftp |
| N1 | MED | H3 introduced dup sha256_of_file across tarball↔OCI boundary both impls (supply-chain hash path); inconsistent w/ existing MAX_COMPRESSED_BYTES/decompress_capped cross-boundary sharing | fixed | hoisted to fetchers.rs pub(crate)/tarball.py public, oci_client imports; kept io::Result-agnostic so each site keeps its slug (tarball DOWNLOAD-FAILED vs OCI PULL-FAILED phase=blob) |
| RustF2 | MED | Rust tarball lost file:// (regression since S4 curl→ureq; C1 guard surfaced); Python still supports it | fixed | fetch_file_scheme in native_http_transport (strip file://, stat-cap, fs::copy to dest Path); RED-verified 3 tests; makes bounded_http comment accurate |
| SecF1 | MED | Rust tempfile scratch dirs world-readable (no 0o700) under umask 022 → exposes unverified bytes + group-writable TOCTOU; Python 0o700 by default | fixed | harden_scratch_dir_permissions #[cfg(unix)] 0o700 both scratch sites; RED-verified mode |
| RustF1 | MED | fetch_oci mints TokenCache per call → no cross-dep reuse; §3.6 doc comment overstated | fixed(doc)+#203 | R2-B discovered PYTHON ALREADY REUSES (built once _build_env, cli.py:749) → #203 is RUST-ONLY. doc corrected both impls; real wiring = de-defer D-RUST-B |
| #201 | MED | HttpDepDeclStore/HttpEntryBundleStore duplicate store both impls | fixed | generic ContentAddressedHttpArtifactStore both impls; concrete stores = thin wrappers, public ctor API stable; DepDecl-only oci:// guard preserved; Rust msg templates normalized (no fixture asserts wording). Py 3939 / Rust 1242 / workspace all ok. CLOSE #201 |
| #202 | MED | Rust extract reads whole (capped) archive into Vec; Python streams member-by-member | fixed | extract_tar now generic over Read (hand-rolled USTAR reader rewritten streaming); CappedReader fuses bomb-cap into read loop; peak mem = largest member. ALL guards re-verified (zip-slip/symlink/hardlink/caps/checksum/PAX). lzma/xz keep buffered path (lzma-rs no incremental Read — scoped). milpa-core 1248 + conformance green. CLOSE #202 |
| #203 | MED | Rust fetch_oci no cross-resolve TokenCache reuse (Python already reuses) | fixed | DefaultRegistry.oci_token_cache: Arc<TokenCache> built once, Arc::clone'd into fetch_oci per dep; test fetch_oci_with_client_shared_cache_across_two_deps_issues_one_token_challenge. CLOSE #203 |
| L1 | LOW | Rust pull_and_extract_oci no dest cleanup on extract fail | fixed | remove_dir_all(dest) on OCI extract failure, symmetric w/ tarball path |
| L2 | LOW | Python 401-retry drops scheme | fixed | _get_with_auth_retry gained scheme param, manifest()/blob() forward it, retry token(scheme=); RED-verified; matches Rust |

### Round 3 (re-review of round-2 + de-defer) — findings

Security round-3: CLEAN (0 new; noted pre-existing empty-Dir file-count gap, not a finding). Rust/design confirmed #201/#202/#203 correct.

| id | sev | finding | status | note |
|----|-----|---------|--------|------|
| P-STREAM | MED | Python safe_extract.py::extract_tar buffers WHOLE archive (raw_archive_bytes=archive.read() L345 + BytesIO L392) = O(decomp_cap) N-worker memory-DoS. My "#202 Rust-only, Python streams" premise was WRONG — R1-23b admits Python deferred it. Fix Python streaming to match #202 | in-progress | round-4 PY4 |
| RustBug | LOW/MED | #202 read_entry_data collapses ALL non-cap errors → EXTRACT-SIZE-LIMIT; corrupt mid-entry gzip payload misreported as bomb (should be FETCH-EXTRACT-FAILED). Siblings skip_exact/read_header distinguish correctly | in-progress | round-4 RS4 |
| RustF3 | LOW | Rust DepDecl store missing oci:// guard Python has (same slug, worse UX — no MILPA_DEP_DECL_DIR guidance). Pre-existing cross-impl divergence | in-progress | round-4 RS4 (parity) |
| (low) | LOW | oci_client.py _acquire_token inline-dups phase-wrap vs _transport_request | in-progress | round-4 PY4 |
| (low) | LOW | Rust no dedicated 401-retry-preserves-scheme test (Python has one) | in-progress | round-4 RS4 |
| (noted) | — | pre-existing: empty-Dir entries don't count toward file-count cap (millions of dirs bounded only by decomp_cap). General safe_extract, not OCI-specific, not a round-3 finding. Surface to Corey | open | not fixing (out of RFC scope; general extraction hardening) |
| (leave) | LOW | Python constructor-binds verify/error-fn vs Rust per-call param (cosmetic decomposition divergence); size-exceeded not phase-tagged in token/manifest (within M1 scope, slug carries url/cap) | wontfix | cosmetic / by-design |

### Round 4 fixes (of round-3 findings) — landed

| id | status | note |
|----|--------|------|
| P-STREAM | fixed | safe_extract.py extract_tar → _extract_tar_streaming; _CappedDecompressStream (mirrors Rust CappedReader) + tarfile "r|" stream mode; peak O(largest member). Pass-1/2 guard loop byte-for-byte unchanged (getmembers()→for member in tf). lzma-alone keeps bounded trial-decode (matches Rust). 45 guard tests green +3 new (bomb capped <5s, never-whole-read proof). Py 3942. UNDER round-4 re-review |
| RustBug | fixed | read_entry_data: UnexpectedEof→EXTRACT-SIZE-LIMIT, genuine io err→FETCH-EXTRACT-FAILED, cap→cap slug; RED-verified corrupt-mid-stream gzip. milpa-core 1255 |
| RustF3 | fixed | Rust DepDecl oci:// guard added w/ MILPA_DEP_DECL_DIR guidance (parity w/ Python); RED-verified |
| (low) oci_client dedup | fixed | _acquire_token routes through _transport_request (single-source phase-wrap) |
| (low) rust scheme test | fixed | manifest_401_retry_reacquires_token_over_original_scheme pin added |

### Round 4 re-review (of round-4 fixes) — findings; round 5 fixes

| id | sev | finding | status | note |
|----|-----|---------|--------|------|
| SEC-TAIL | MED (agent said HIGH; downrated) | streaming extract (BOTH impls) bypasses Layer-1 decomp-bomb cap for compressed data appended AFTER tar terminator — tarfile "r|"/TarEntries::next stop at zero-block, tail never read/decompressed/capped. OLD _decompress_capped read cap+1 unconditionally. Empirically repro'd (2GiB tail, extract succeeds). DOWNRATE rationale: NOT OOM (tail never decompressed) + milpa digest-gates OCI/pinned archives + hashes EXTRACTED TREE (tail never enters) → non-exploitable in practice; but real broken defense-in-depth guarantee, fail-closed bar → fix | in-progress | round-5 PY5+RS5: drain capped stream after tar end so cap covers whole stream |
| N2 | MED | Python lzma-alone branch REIMPLEMENTS _decompress_capped inline (Rust delegates to shared helper); dup of security-sensitive cap logic + stale docstring | in-progress | round-5 PY5: call _decompress_capped |
| (clean) | — | round-4: format detection, forward-only "r|", _CappedDecompressStream boundary, callers, identity, _acquire_token dedup ALL verified correct. RS4 read_entry_data error-map re-confirmed sound. Python streams xz too (exceeds Rust #202 which buffers xz — intentional, lzma-rs no incremental Read) | verified | — |
| (note) | LOW | Python decompressor objs not explicitly closed (harmless — underlying file closed in finally); worth a comment | wontfix/optional | non-blocking |

### Round 5 fixes (of round-4 findings) — landed; round-5 security-confirm running

| id | status | note |
|----|--------|------|
| SEC-TAIL (py) | fixed | _drain_capped_stream() drains after tarfile block so cap covers WHOLE stream; RED-verified (500KB tail→EXTRACT-SIZE-LIMIT); legit padding non-regressed. Py 3944 |
| SEC-TAIL (rust) | fixed | drain_to_eof() after pass-1 loop reads same CappedReader; BONUS factored classify_stream_read_error() reused by read_header/read_entry_data/skip_exact/drain (removed 3 inline copies, no 4th). RED-verified (1MiB tail→EXTRACT-SIZE-LIMIT). milpa-core 1257 |
| N2 (py) | fixed | lzma-alone branch now calls _decompress_capped (single source); docstring corrected |

Round-5 re-review: security-confirm agent running (terminating confirmation). If clean → loop floor reached (0 Crit/High/Med), close #201/#202/#203, report.

## STAGE 4 CODE-REVIEW: COMPLETE (loop floor reached)

Round 5 terminating security-confirm: bomb-cap trailing-data bypass FULLY CLOSED both impls, no regression, no new issue. 5 review rounds total; loop floor = 0 Crit/High/Med.

Totals: 1 Critical (C1 SSRF), 3 High (H1/H2/H3 + Bug1), ~15 Medium, several Low — ALL fixed. Issues #201/#202/#203 FIXED+CLOSED (not deferred, per "stop deferring"). #200 (private-registry creds/diversity) remains the one legitimate future-scope defer.

Remaining (wontfix/cosmetic, below loop floor): Python constructor-bind vs Rust per-call store param (style); size-exceeded not phase-tagged in token/manifest (within M1 scope); Python decompressor objs not explicitly closed (harmless). Pre-existing note surfaced (NOT fixed, general safe_extract hardening not OCI-specific): empty-Dir entries don't count toward file-count cap.

Residual cross-impl divergences (acceptable, documented): (a) Content-Length declared>cap — Python accepts complete-small-body, Rust ureq rejects (both safe, digest gates); (b) xz extraction — Python streams, Rust buffers (lzma-rs no incremental Read).

Gates final: Python 3944 passed / Rust milpa-core 1257 + conformance + workspace green.
STILL UNCOMMITTED — commit only on Corey's request. Manual softlink smoke still pending (network-restricted env).
