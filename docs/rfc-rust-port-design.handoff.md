# Rust-port design RFC — handoff

- **Stage:** 3 (slice grind) — **P1–P4 (main). S0 (main 94c524f). S1 (rust 6de9646). S2 harness+CI (rust a05697b). S3 manifest grammar (rust 2bf4fe5). S4 identity+CAS (rust 09323f7). S5a lockfile parse (rust 2d95579). S5b lockfile emit (rust aedcb65). S6 version/VersionSet/Strategy/Profile (rust 36a7210). S7a solver core (rust 63cf37c). S7b resolver orchestration (rust a6ce3bc). S7c emission glue + from_graph (rust 3f28406). S8 index/TNG reader (rust c1e2808). S9 nim.cfg emission (rust 718ad2c). S10 frozen path (rust e48caee). S11a workspace loader (rust 14ce9fb). S11b workspace resolve + frozen + per-member nim.cfg + ResolvedDep→ProvenanceRecord model change DONE+GREEN+COMMITTED. On `rust` branch. **Corpus 115/117 pass — only 2 parked (099 res-provenance-conflict, 115 conditional-dep-excluded).** S12 bijection lint (rust e421aba). S13a env→Profile (rust efde62b). S13b discovery+verify (rust 35cd1bb). S13c format_manifest+mutation (rust 782a0ff). S13d-cache index cache (rust 9d64db0). S14a resolver gate-ordering fix → **fixture-099 greens → CORPUS 117/117, known_failing.txt EMPTY** (the RFC's primary Done milestone: full spec-v1 suite green). On `rust` branch. S14b safe_extract (rust 28f5384). S14c-1 Local+Git (rust e7decb9). S14c-2 tarball+oci (rust aa65210). milpa-cli binary + add --mirror (last catalog code) DONE+GREEN+COMMITTED. **CORPUS 117/117; error catalog is a PURE BIJECTION (implemented∪exempt==spec, DEFERRED empty); all 8 CLI verbs + all 4 transports implemented.** RFC S1–S14 complete. Only **S15 stretch** (differential harness) remains.**   •   **Round:** 2/2
- **LOOP MODE:** Corey switched to autonomous grind — DON'T pause per slice; do a slice, commit, report one line, ScheduleWakeup ~15 min, continue. He compacts occasionally. Still pause only on a genuine fork or wrong-spec escalation.
- **COMMIT CADENCE: standing approval granted** — commit each slice on `rust` as it greens, no re-ask; still pause on forks/wrong-spec.
- **S3 result (DONE, rust 2bf4fe5):** full milpa.kdl package+workspace grammar in milpa-manifest, mirroring milpa/manifest.py. Five dep forms + predicates (inline/child-OR/when-AND) + flags/overrides/mirrors/cas/spec-version/dev-deps + every structural MAN-* code. **kdl-rs = KDL 1.0 via `parse_v1`** (v2's `parse` decodes bare true/false as strings — confirmed against fixtures 027/038/040/045; enabled the `v1` cargo feature → pulls kdl 4.7.1 backend, in Cargo.lock). `ManifestError` is now one struct `{code:&'static str, message}` (mirrors Python's coded exception); `all_codes()` lists the 62 structural slugs. **Deliberate spec-correct divergence:** `url_arg` rejects non-`(url)` annotations on URL positions (MAN-URL-ARG-TYPE) per grammar §2 — Python ignores annotations; no fixture exercises it. milpa-core re-exports `parse_document`/`ManifestDoc`. MilpaTarget::Resolve parses → surfaces MAN-* / falls through to NOT_WIRED. Corpus 62 pass / 55 xfail / 0 regressions.
- **S4 result (DONE, rust 09323f7):** `identity.rs` = `compute_content_hash` (canonical byte stream §1.2 — relpath/mode/content with 0x00 seps, raw-byte relpath sort §1.3, .git-exclude §1.4, exec-bit §1.7, symlink-by-target §1.5, no CRLF norm §1.6) + `parse_identity` (4 ID-* codes; **ID-NOT-A-STRING unreachable** — input is `&str`) + ID-NON-UTF8-SYMLINK-TARGET. `store.rs` = CaStore admit (**move-via-rename** per spec §3.3 — the S4-line "copy-then-admit" paraphrase was wrong; spec+Python use rename; copy-then-admit is the S10 TempCaStore/cas-seed concern), CAS-IDENTITY-MISMATCH, duplicate=no-op, link (relative symlink §3.5, CAS-NOT-IN-STORE §3.6, idempotent clear_dest mirroring fsutil.py), default_store tiers 1/3/4. **Deliberate non-catalog choice:** mid-hash filesystem I/O failures are uncoded in spec §5 (Python propagates raw OSError) → rendered as `MILPA-INTERNAL-IO` sentinel, kept OUT of all_codes(). all_codes() now declares 5 ID-* + 2 CAS-* (subset; bijection at S12). **Byte-parity vector pinned against the Python oracle** (mixed regular/exec/symlink/.git tree → efa2…60b9). 26 core tests green; conformance corpus UNCHANGED (no fixture greens at S4 — unblocks first success fixture at S9). `compute_content_hash`/`parse_identity`/`default_store`/`SUPPORTED_ALGORITHMS` re-exported from milpa-core.
- **S5a result (DONE, rust 2d95579):** `lockfile.rs` = `parse_lockfile(text)->Lockfile` (KdlDocument::parse_v1, KDL-1.0 same as S3) + `load_lockfile(path)` disk wrapper. **New milpa-types data model** (the S1 scaffold wrongly reused `Vec<ResolvedDep>` for `Lockfile.deps`): `LockedDep` (identity is `Option<String>` — Phase A partial; + active_flags/self_mirrors) and `ProvenanceRecord` — a **6-kind** enum (Git/Tarball/Local/Member/Oci/Registry) deliberately SEPARATE from the **4-kind** transport `Provenance` (Member=workspace-internal, Registry=read-compat #97; neither is a transport). `Lockfile` gains `version:u32` + hand-written `Default` (v1, not derived 0); `LOCKFILE_SCHEMA_VERSION` const. **`Milpa` impl type** added to lib.rs carrying `LockfileParser`; Resolver(S7)/FrozenResolver(S10) land on the SAME type. identity validation reuses `parse_identity`, remapping any ID-* → LOCK-DEP-IDENTITY-INVALID. **Deliberate spec-correct strictness:** `version` requires a true KDL `Integer` → a numeric *string* is LOCK-FIELD-TYPE (Python's `int("1")` would coerce; spec §2.1 says non-integer→type-error, Rust is more conformant). CoreError grew 14 LOCK-* slugs + a `message()` accessor; all_codes() includes LOCK-FILE-NOT-FOUND/UNREADABLE (unit-tested, not fixture-expressible). 12 LOCK-* fixtures (066-077) greened+removed → corpus **74 pass / 43 xfail / 0 xpass / 0 regressions**. 33 milpa-core unit tests green; clippy+fmt clean.
- **S5b result (DONE+GREEN, uncommitted at write — committing now):** `lockfile.rs` gained `format_lockfile(&Lockfile)->String` + `format_provenance_fields` + `kdl_str` + `write_lockfile(&Lockfile,&Path)->PathBuf`, all byte-exact mirrors of `lockfile.py:format_lockfile`/`_format_provenance_fields`/`_kdl_str`/`write_lockfile`. Header SSOT'd as `const HEADER`. **Sorting NOT done in the formatter** (correcting the earlier handoff note): Python sorts deps-by-name + requires-lexicographically in `from_graph`/`_locked_from_resolved`, NOT in `format_lockfile` — so the Rust formatter is a pure structural render and a `Lockfile` reaches it already canonical. Per-kind field order + optional-omit + `(url)` self_mirrors + final `\n` all matched. **R11 escaping** via the single `kdl_str` (named escapes + `\u{..}` for residual controls). write_lockfile I/O failures → non-catalog `MILPA-INTERNAL-IO` sentinel (kept OUT of all_codes(), same as S4 identity/CAS I/O). `format_lockfile`/`write_lockfile` re-exported from milpa-core. **`from_graph(ResolvedGraph)->Lockfile` bridge DEFERRED to S6/S7** — it needs `Version`→semver formatting (`_format_version_str`) which is S6; pure emit of a hand-built Lockfile does not, so S5b shipped without it. 7 new emit unit tests (byte-exact, empty, optional-omit, escaping round-trip, parse→format→parse identity + format fixed-point, every-prov-kind round-trip, write-creates-parents). 40 milpa-core tests green; clippy+fmt clean; **corpus UNCHANGED 74 pass / 43 xfail / 0 xpass / 0 regressions** (S5b greens no fixture alone; first success = S4+S5b+S6+S7b+S7c+S9, §9; fixture-118-lock-string-escaping greens once the resolve path is complete).
- **S6 result (DONE+GREEN, rust 3f1cdcd):** **milpa-types `Version` redesigned** from the S1 `{components,raw}` scaffold to the full semver model `{major,minor,patch,pre:Vec<PreId>,build:String}` + `PreId{Numeric(u64)|Alpha(String)}`, with **hand-written `Ord`/`Eq`/`Hash` over a `precedence_key`** (build ignored §10; release > prerelease via `is_release` flag §11.3; numeric<alpha via `PreId` variant order §11.4.3; larger-set-wins via `Vec` cmp §11.4.4). Doc-comment updated: a value type owning its own total order is NOT the "zero-logic" rule's forbidden algorithm logic, and the orphan rule forces the impls to live with the type; the *resolution* algebra still lives in milpa-solver. **milpa-solver** got `parse_version`(→`Option<Version>`, hand-rolled semver grammar, NO regex/proptest deps added — network-safe; mirrors Python returning `None` for non-canonical), `format_version_str` (lossless inverse modulo dropped `v`-prefix + leading-zero normalization), the **full `VersionSet` interval algebra** (full/empty/gte/gt/lt/lte/eq closed-singleton P3.1b/from_constraint with `||`/`|`/`&` precedence + `~`/`^`/`!=`/`==`/`=` ops, contains/intersect/union/complement/is_subset_of/lower_bound + normalize/connectable/bound helpers — faithful port of solver.py), and **`Strategy` gained `Semver`** (as_str "semver"). Constraint-parse failures use a NEW **uncoded `ConstraintError`** (mirrors Python's bare `ValueError`; the resolver maps it to `MAN-NIMBLE-CONSTRAINT` at S7b — solver owns no slug, stays out of all_codes). **Profile placement CONFIRMED** (milpa-manifest, data-only; stale "predicate eval lands in S6" comment fixed → S7b/fixture-115). Tests: 20 milpa-solver (incl. the **#63 lo=None merge-gap regression** `lt(v).union(full())==full()`, parse/format round-trips, semver-prerelease order, P3.1b singleton, tilde/caret/!=/disjunction, **bounded-exhaustive algebraic laws** — idempotency/identity/involution/commutativity/De Morgan/subset/contains-via-intersect over a small version domain, the dependency-free stand-in for the Python Hypothesis suite) + 5 milpa-types (semver order, prerelease<release, build-ignored eq/hash, numeric<alpha). milpa-types 5 / milpa-solver 20 / milpa-core 40 / milpa-manifest 14 green; clippy `-D warnings` + fmt clean; **corpus UNCHANGED 74 pass / 43 xfail / 0 xpass / 0 regressions** (S6 greens no fixture alone, §9).
- **S13a result (DONE+GREEN, rust efde62b):** **env→Profile + conditional-dep predicate filtering** (the corpus-relevant atom of S13, split out so it greens its fixture independently of the large CLI-binary body). `Profile` (milpa-manifest) gained `platform: Option<String>`, `arch: Option<String>`, `milpa_version: Option<Version>` (joining `nim_version` + `flags`) — the four `MILPA_TARGET_*` axes of the Python `Profile`, each `Option` so an absent axis matches no predicate of its name. Resolver `predicate_satisfied` now evaluates `platform`/`arch` as plain string-equality (Nim hostOS/hostCPU vocab) + `milpa` as a version constraint (like `nim`); the prior `_ => false` catch-all for these is gone. **Harness:** new `fixture_profile(dir)` reads the fixture's optional `env` file (KEY=VALUE `MILPA_TARGET_*` lines, mirrors Python `_fixture_profile`) → builds a `Profile` → passes it to both the package `Milpa::resolve` and the workspace `resolve_workspace` (was `None`). `Profile` + `parse_version` re-exported from milpa-core (resolved a `Profile`-imported-twice conflict by making the milpa-manifest import a single `pub use`). **Greened fixture-115-conditional-dep-excluded** (a `platform "windows"`-gated dep, env `MILPA_TARGET_PLATFORM=linux` → excluded → empty graph + header-only nim.cfg). Tests: existing resolver conditional-dep test updated to `..Profile::default()`. **milpa-core 101** / milpa-manifest 23 / others unchanged; clippy `-D warnings` + fmt clean; **corpus 116 pass / 1 xfail / 0 xpass / 0 regressions** (was 115/2). **Only 099-res-provenance-conflict remains parked** (the S14 gate-ordering fix). No new error codes (env→Profile emits none), so the bijection lint is unchanged.
- **S13b result (DONE+GREEN, rust fc35515):** **manifest discovery + verify (the read-side library of S13).** New `milpa-core/src/discovery.rs` = `load_manifest(path)` (explicit path → MAN-FILE-NOT-FOUND / MAN-FILE-UNREADABLE / parser MAN-*) + `discover_manifest(dir)` (milpa.kdl → `<dir>.nimble` → single `*.nimble` → MAN-NIMBLE-AMBIGUOUS on multiple → MAN-NO-MANIFEST on none) + `manifest_from_nimble(&NimbleManifest, name)` (URL req→UrlDep, named→NamedDep, `nim` dropped, kind=library; mirrors `manifest.py`). **Key exemption insight (corrected S12's DEFERRED):** the Rust `.nimble` parser is a *total* heuristic scan (never raises) and reads text not files, so **MAN-NIMBLE-PARSE is unreachable** and a `.nimble` read failure surfaces as **MAN-FILE-UNREADABLE** (the standalone NIMBLE-FILE-* are never emitted — Python's `load_nimble` raising them is dead outside its tests, P2). So the bijection lint moved MAN-NIMBLE-PARSE + NIMBLE-FILE-NOT-FOUND + NIMBLE-FILE-UNREADABLE from DEFERRED → **EXEMPT** (3 codes), and MAN-FILE-NOT-FOUND/MAN-FILE-UNREADABLE/MAN-NO-MANIFEST/MAN-NIMBLE-AMBIGUOUS (added to `ManifestError::all_codes`) + LOCK-GRAPH-MISMATCH (added to `CoreError::all_codes`) → **implemented** (5 codes). **verify** added to `lockfile.rs`: `verify_against_graph(&Lockfile,&ResolvedGraph)->Result<(),LOCK-GRAPH-MISMATCH>` (lockfile⟷resolve dep-for-dep) + `verify_lockfile_against_deps(&Lockfile,&Path)->Vec<String>` (on-disk identity divergence report, no single code — `milpa verify`'s primitive). All re-exported from milpa-core. Tests: +7 discovery + 2 verify. **milpa-core 110** / milpa-manifest 23 / others unchanged; clippy `-D warnings` + fmt clean; **corpus UNCHANGED 116 pass / 1 xfail / 0 xpass / 0 regressions** (S13b greens no fixture — discovery/verify codes aren't fixture-expressible; the corpus reads manifest text directly + drives the Target paths, not the CLI). DEFERRED now = the 4 mutation codes (S13c) + the S14 FETCH-*/EXTRACT-* codes.
- **S13c result (DONE+GREEN, rust 782a0ff):** **`format_manifest` + manifest mutation** (the write-side library of S13). New `milpa-manifest/src/format.rs` = `format_manifest(&Manifest) -> String` (the inverse of the package parser; byte-mirror of `manifest.py:format_manifest` + `_format_dep_line` + `_quote_name`): normative header, then spec-version/name/src_dir/cas/deps/dev-deps/overrides/mirrors/flags blocks (each omitted when empty), trailing `kind`; URL/tarball/override/mirror values carry the `(url)` annotation; inline vs child predicate emission + mirrors + flag-requests as a `{ }` block; **insertion-stable dep order** (grammar §8). Re-exported from milpa-manifest + milpa-core. New `milpa-core/src/manifest_writer.rs` = `mutate_manifest_file(path, FnOnce(Manifest)->Manifest) -> WriteResult` + `write_manifest(&Manifest, path)` (atomic temp+rename): the mutation guards — missing file → **MAN-MUTATE-FILE-NOT-FOUND**, `.nimble` → **MAN-MUTATE-NIMBLE-REFUSED**, workspace doc → **MAN-MUTATE-WORKSPACE-REFUSED** (via `parse_document` discriminant, cleaner than Python's text-scan) — plus a `WriteResult{path, comments_lost}` carrying the count of `//`-comment lines dropped by the declarative re-render (the formatter keeps only its 1 header). 3 MUTATE codes added to `ManifestError::all_codes`. Manifest-write I/O failure → the non-catalog `MILPA-INTERNAL-IO` sentinel. Tests: +3 milpa-manifest format (minimal round-trip, url+named `(url)` round-trip, no-deps-omits-block) + +5 milpa-core manifest_writer (each MUTATE code + add-dep-rewrites-canonically-with-comment-loss + malformed→MAN-NAME-MISSING). **milpa-core 115** / milpa-manifest 26 / others unchanged; clippy `-D warnings` + fmt clean; **corpus UNCHANGED 116 pass / 1 xfail / 0 xpass / 0 regressions** (mutation codes aren't fixture-expressible). Bijection lint DEFERRED now = **MAN-ADD-MIRROR-IDENTITY-MISMATCH** (S13d — `add --mirror` needs to fetch both URLs + compare identities) + the S14 FETCH-*/EXTRACT-* codes only.
- **CLI binary result (DONE+GREEN, rust ca94cb4) — 🎉 RFC S1–S14 COMPLETE + PURE BIJECTION:** **the `milpa` CLI binary + `add --mirror`** (the last catalog code). `milpa-cli/src/main.rs` (replaces the stub): hand-rolled argparse over `std::env::args` (no clap) — global flags `-C`/`--directory`, `-j`/`--parallel` (accepted, ignored — serial reference is `-j`-independent §4.4), `-s`/`--strategy`, `--frozen`, `--version`; then verb + tail. Exit 0/1 with a single `CODE: message` stderr diagnostic (cli-contract §3/§4); verb data to stdout. The 8 verbs are thin glue over milpa-core: **show** (parse_lockfile→print), **verify** (verify_lockfile_against_deps→report), **clean** (rm `_deps/`+`nim.cfg`, keep lock), **fetch/lock/update** (discover_manifest → Milpa::resolve / resolve_frozen / resolve_workspace via `DefaultRegistry::with_curl` + index cache → write lockfile [+ nim.cfg]), **add** (`--git`→append UrlDep via mutate_manifest_file; `--mirror`→`add_mirror`), **remove** (drop by name). Env: `MILPA_INDEX_URL`/`MILPA_CACHE_DIR`/`XDG_CACHE_HOME`. **`add_mirror`** (new in milpa-core `manifest_writer`): fetch mirror at the dep's ref → content-hash → compare to the dep's **locked identity** → **MAN-ADD-MIRROR-IDENTITY-MISMATCH** if divergent, else record the mirror (the identity-gate invariant). MAN-ADD-MIRROR added to `ManifestError::all_codes`; **the bijection lint's DEFERRED is now EMPTY → `implemented ∪ exempt == spec`, a pure bijection.** Tests: +2 add_mirror (accept identical / reject divergent — local git repos + a dummy https canonical so the manifest parses; `file://` only for the fetched mirror) + 3 milpa-cli (arg parse, add-args split, offline show/clean/add/remove round-trip). `tempfile` milpa-cli dev-dep. **milpa-core 145** / milpa-cli 3 / milpa-manifest 26 / others unchanged; clippy `-D warnings` + fmt clean; **corpus 117/117, 0 regressions.** **RFC §6 S1–S14 + the CLI are complete; only S15 (stretch differential harness) remains.**
- **S14c-2 result (DONE+GREEN, rust aa65210):** **tarball + oci transport fetchers** (`fetchers.rs`) — completes the real `DefaultRegistry` (all 4 transports). **Tarball** (`fetch_tarball`): download via the injected `HttpGet` seam (`DefaultRegistry::with_curl` for production; tests inject a closure) → verify declared sha256 over the bytes **before** extraction (**FETCH-SHA256-MISMATCH**, accepts bare or `sha256:`-prefixed) → gzip-magic (1f 8b) detect + decompress (**flate2** — added; pure-Rust miniz_oxide backend) → `safe_extract::extract_tar` with strip_components, wrapping any failure as **FETCH-EXTRACT-FAILED**; transport error → **FETCH-DOWNLOAD-FAILED**. **OCI** (`fetch_oci`): `oras pull` to a scratch dir (**FETCH-OCI-PULL-FAILED**, incl. oras-absent) → locate the single `*.tar.gz` (0 → **FETCH-OCI-NO-TARBALL**, >1 → **FETCH-OCI-AMBIGUOUS-TARBALL**) → gunzip + extract_tar. `DefaultRegistry` now carries the `HttpGet` (so it has a field — the unit-struct derives are gone; `new(http_get)` + `with_curl()`). 6 codes added to `FetchError::all_codes` + cleared from the lint's DEFERRED. Tests: +6 (tarball raw-tar / gzip / sha256-mismatch-rejects-before-extract / sha256-match / download-failed; oci-pull-failed when oras absent) — gzip tested via a `flate2` GzEncoder round-trip, offline. **`flate2 = "1.1.9"`** added to milpa-core (network was available; pure-Rust backend, no C). **milpa-core 143** / others unchanged; clippy `-D warnings` + fmt clean; **corpus UNCHANGED 117/117** (FETCH codes aren't fixture-expressible). **All transport fetchers are now real.** Bijection lint DEFERRED = just **MAN-ADD-MIRROR-IDENTITY-MISMATCH** (the milpa-cli `add --mirror` verb — the only remaining catalog code).
- **S14c-1 result (DONE+GREEN, rust e7decb9):** **Local + Git fetchers + the real `DefaultRegistry`** (`milpa-core/src/fetchers.rs`; the dispatch deferred since S8). `DefaultRegistry: FetcherRegistry` matches the closed `Provenance` enum → per-transport fetch. **Local** (`fetch_local`): pure dir copy (symlink-preserving), **FETCH-LOCAL-PATH-NOT-FOUND** / **FETCH-LOCAL-PATH-NOT-DIR**. **Git** (`fetch_git`): `git` CLI subprocess clone + checkout pinned commit (cat-file presence check → **FETCH-GIT-COMMIT-ABSENT**; clone/checkout nonzero → **FETCH-GIT-FAILED**), resolved_ref = `rev-parse HEAD`. Both **offline-testable** (Local pure; Git against *local* repos — git 2.54 in the container, no network). Tarball/OCI dispatch arms return a non-catalog placeholder (S14c-2). New `FetchError::Transport(&'static str, String)` variant carries the coded FETCH-* failures; `fetch_msg` updated. 4 codes added to `FetchError::all_codes` + out of DEFERRED. **FETCH-RECEIPT-EMPTY → EXEMPT**: the Rust typed `Receipt` struct makes an evidence-less receipt unrepresentable (Python's code guards its dict-based receipt) — same type-enforced exemption as ID-NOT-A-STRING. `DefaultRegistry` re-exported. Tests: +7 (local copy/not-found/not-dir; git clone+pin / nonexistent-repo→FAILED / bogus-pin→COMMIT-ABSENT; registry-dispatches-local) — git tests build a throwaway local repo with `-c user.*` (safe: disposable fixture, not the milpa repo). **milpa-core 137** / others unchanged; clippy `-D warnings` + fmt clean; **corpus UNCHANGED 117/117**. DEFERRED now = MAN-ADD-MIRROR-IDENTITY-MISMATCH + tarball(3) + oci(3) = 7.
- **S14b result (DONE+GREEN, rust 28f5384):** **safe tar extraction** (`milpa-core/src/safe_extract.rs`, mirrors `fetchers/safe_extract.py:extract_tar`). `extract_tar(tar_bytes, dest, strip_components, Limits) -> ExtractionResult` with the three guard classes: **EXTRACT-ZIP-SLIP** (entry path escapes `dest` via `..`/absolute — lexical normalize + prefix check, since the target doesn't exist yet so `canonicalize` can't be used), **EXTRACT-SYMLINK-ESCAPE** (symlink target resolves outside `dest`), **EXTRACT-SIZE-LIMIT** (per-file / total-bytes / file-count caps — decompression-bomb defense). `strip_components` drops leading path components (strip-before-hash: the caller content-hashes the extracted tree). **Hand-rolled USTAR reader** (no `tar`/`flate2` crate — those aren't workspace deps and adding them needs network; pure + offline, same ethos as the `.nimble` parser + nkdl). Operates on decompressed tar bytes; **gzip/xz decompression is the tarball fetcher's job (S14c)** — it decompresses then calls `extract_tar`. Carried by the existing `FetchError::Extract(code, msg)` variant; the 3 EXTRACT codes added to `FetchError::all_codes` + removed from the bijection lint's DEFERRED. Re-exported (`extract_tar`/`ExtractionResult`/`Limits`). Tests: +8 (files+dirs, strip_components, zip-slip via `..`, symlink-escape, in-tree-symlink-allowed, per-file/total/count caps) with hand-built USTAR archives. **milpa-core 130** / others unchanged; clippy `-D warnings` + fmt clean; **corpus UNCHANGED 117/117** (EXTRACT codes aren't fixture-expressible — the corpus uses the FakeFetcher, no real archives). DEFERRED now = MAN-ADD-MIRROR-IDENTITY-MISMATCH + the 11 FETCH-* codes (S14c).
- **S14a result (DONE+GREEN, rust 8b08ddd) — 🎉 CORPUS 117/117:** **resolver BFS gate-ordering fix** → greens fixture-099-res-provenance-conflict, the last parked fixture → **full spec-v1 conformance corpus passes, `known_failing.txt` empty** (RFC §6 "Done"). The fix: `process_item` (gate-then-fetch, per item) was split into `process_items(Vec<Item>)` = **gate ALL sibling items first, then dispatch survivors** (+ `gate_only` / `dispatch` helpers). Previously two conflicting transitive `requires` of one parent (099: pkga requires `…/example/sharedlib.git` + `…/other/sharedlib.git` — same derived name, different URLs) failed on the FIRST sibling's unmocked fetch (FETCH-ALL-FAILED) before the second sibling's gate fired; now both are gated before either is fetched, so the **RES-PROVENANCE-CONFLICT** wins (matching the Python thread-pool `submit()` which gates synchronously before collecting fetch results). All 6 call sites (2 top-level queues + 4 worker sub_items loops) route through `process_items`. Gate semantics unchanged (first-claim-wins per name), just batched per-parent — no other fixture/test affected. **milpa-core 122** / others unchanged; clippy `-D warnings` + fmt clean; **corpus 117 pass / 0 xfail / 0 xpass / 0 regressions** (was 116/1). Remaining S14 (catalog codes + integration tests, NO new corpus fixtures): safe_extract (EXTRACT-*), real fetchers (FETCH-*).
- **S13d-cache result (DONE+GREEN, rust 9d64db0):** **the 4-state tianguis index cache** (deferred from S8; the bounded, fully-testable piece of S13d). New `milpa-core/src/index_cache.rs` = `load_index(url, cache_dir, http_get, ttl_seconds, now_unix) -> Result<Index, MilpaError>` mirroring `tianguis_client.load_index`: **fresh** (age<ttl → cached, no network), **stale** (age≥ttl → refetch+overwrite), **missing** (fetch+populate), **offline-fallback** (fetch fails + any cache exists → serve it). HTTP transport (`HttpGet = &dyn Fn(&str)->Result<String,String>`) + `now_unix` clock are **injected** so all four states are unit-tested without network/wall-clock. **Deterministic-age design:** fetch time recorded in a sidecar `<hash>.at` file (NOT fs mtime — avoids `File::set_modified` which is 1.75>MSRV 1.74, and makes age governed by the injected clock). Atomic write (temp+rename). Cache key `sha256(url)[..16].index.kdl`. `DEFAULT_INDEX_URL` const + `DEFAULT_TTL_SECONDS` (24h) + `index_url_from_env()` (MILPA_INDEX_URL override). Index-acquisition failure with no fallback → non-catalog `MILPA-INDEX-UNREACHABLE` sentinel (kept out of all_codes, like MILPA-INTERNAL-IO); a cached/fetched parse error surfaces the real TNG-* code. The cache lives in the global index dir, **never** evicted by `milpa clean` (it is the registry, not project state). Re-exported from milpa-core. Tests: +7 (missing→fresh-one-fetch, stale-refetch, offline-fallback-serves-stale, offline-no-cache→UNREACHABLE, fetched-parse-error→TNG-SCHEMA-UNKNOWN, stable url-derived key, env-default). **milpa-core 122** / others unchanged; clippy `-D warnings` + fmt clean; **corpus UNCHANGED 116 pass / 1 xfail / 0 xpass / 0 regressions** (no new catalog code; the cache is infra). Bijection lint DEFERRED unchanged (MAN-ADD-MIRROR-IDENTITY-MISMATCH + S14 FETCH-*/EXTRACT-*).
- **milpa-cli binary plan (after S14, per the sequencing note):** the `milpa-cli` binary proper — argparse-equivalent for the 8 conformance verbs (fetch/lock/show/verify/clean + add/remove/update), global flags (`-C`/`-j`/`-s`/`--frozen`/`--version`), exit-code taxonomy (§3), stdout-vs-stderr routing (§4), env vars (§8, incl `MILPA_INDEX_URL`/`MILPA_CACHE_DIR`), manifest auto-discovery (milpa.kdl → `.nimble` fallback — emits MAN-FILE-NOT-FOUND/UNREADABLE/MAN-NO-MANIFEST/MAN-NIMBLE-PARSE/MAN-NIMBLE-AMBIGUOUS/NIMBLE-FILE-*), the manifest-mutating verbs (add/remove/update) incl **`format_manifest` in milpa-manifest** (comment-drop warning + insertion-stable dep order + `(url)` annotation, grammar §8 — emits MAN-MUTATE-FILE-NOT-FOUND/MAN-MUTATE-NIMBLE-REFUSED/MAN-MUTATE-WORKSPACE-REFUSED/MAN-ADD-MIRROR-IDENTITY-MISMATCH), and **`milpa verify`** (LOCK-GRAPH-MISMATCH via a `verify_against_graph`-equivalent), plus the **4-state index cache** deferred from S8 (the CLI's `_default_index_loader` is its consumer). Mirror `milpa/cli.py` + `manifest.py:format_manifest` + `tianguis_client.load_index`. **Greens NO corpus fixtures** (the corpus drives the `Target` paths, not the CLI binary) — its payoff is moving the 12 S13 DEFERRED codes into `all_codes()` (bijection lint progress) + unit tests + making the tool actually runnable. As each code becomes emittable, delete it from the lint's `DEFERRED` list.
- **S12 result (DONE+GREEN, rust e421aba):** **error-catalog bijection lint + conformance declaration.** Flipped the corpus parity test from `implemented ⊆ spec` to a **partition bijection** (`rust_error_catalog_is_a_bijection_with_the_spec`): every one of the 148 `docs/spec/errors.md` codes has exactly one home — `implemented_error_codes()` (emittable now), `DEFERRED` (tagged with the slice that wires it), or `EXEMPT` (never emitted). Asserts: no orphans (implemented ⊆ spec), the 3 buckets pairwise disjoint, DEFERRED/EXEMPT reference only real spec codes (no stale entries), and union == spec (exhaustive — a new spec code with no home fails the lint). **The lint immediately earned its keep:** it caught that the 3 `RES-WS-*` codes were *emitted* by `resolve_workspace` (S11b) but missing from `CoreError::all_codes()` — added them (S11b oversight, now fixed). `DEFERRED` (26 codes): S13 = MAN-FILE-*/MAN-NO-MANIFEST/MAN-NIMBLE-PARSE+AMBIGUOUS/MAN-MUTATE-*/MAN-ADD-MIRROR-IDENTITY-MISMATCH/NIMBLE-FILE-*/LOCK-GRAPH-MISMATCH (CLI discovery + mutation verbs + verify); S14 = all FETCH-* (except FETCH-ALL-FAILED, already wired) + EXTRACT-* (real fetchers + safe-extract). `EXEMPT` (2): ID-NOT-A-STRING (unreachable — `parse_identity` takes `&str`), TNG-BAD-VERSION (reserved, raised by neither impl). The non-catalog `MILPA-INTERNAL-IO` sentinel is in no bucket (absent from errors.md by design). When DEFERRED empties (post-S14) the lint is a pure `implemented ∪ exempt == spec`. **Conformance declaration** added to `milpa-conformance/Cargo.toml` `[package.metadata.milpa]`: `spec-version="1.0"`, `corpus="spec-v1"`, `known-failing="known_failing.txt"` (the corpus test reads the latter directly). No new fixtures green (S12 is a lint, not a feature); **corpus UNCHANGED 115 pass / 2 xfail / 0 xpass / 0 regressions**; milpa-core 101 / conformance 11+2+4 green; clippy `-D warnings` + fmt clean. **Parked-fixture assessment (per Corey's ask — NEITHER is a quick win):** 099-res-provenance-conflict needs a resolver BFS restructure — the serial resolver fetches the first sibling sub-item (an unmocked `sharedlib` URL → FETCH-ALL-FAILED) *before* the second sibling's gate fires the RES-PROVENANCE-CONFLICT; the spec/Python gate all siblings before fetching, so this needs gate-all-then-fetch enqueue ordering in the BFS (a focused S7b-style fix, filed mentally for a follow-up). 115-conditional-dep-excluded needs `Profile` platform/arch fields + the harness building a `Profile` from env (it passes `None` today) — that's the §6 env→Profile wiring that lands with S13's CLI. Both stay parked, documented.
- **S11b result (DONE+GREEN, rust 2d84732):** **workspace resolve + frozen + per-member nim.cfg, on top of a clean model change.** (1) **Model change — `ResolvedDep.provenance: Provenance` → `ProvenanceRecord`** (the post-resolution record IS emission-level; honors the S5a transport-vs-record separation rather than hacking `Member` into the 4-kind transport enum). The transport→record map moved from `from_graph` into resolver `build_graph` (new `transport_to_record` + `opt` helpers; `Candidate.provenance: Option<ProvenanceRecord>`); `from_graph` is now a clone; frozen `resolved_from_locked` is a record-clone + Registry-rejection. Updated nimcfg_tests/lockfile-tests/resolver_tests assertions `Provenance::`→`ProvenanceRecord::` (git ref_spec now `Option`). (2) **`resolve_workspace`** (resolver.rs): new `seed_workspace` pre-registers each member as a `ProvenanceRecord::Member` Candidate (identity = `compute_content_hash(member.directory)`, NO fetch), seeds root with member terms + member-coercion (a `member "X"` dep or a bare named dep matching a member → in-tree member, no index lookup — new `member_names` set short-circuits `process_named`), and reuses the S7b BFS/gate/finalize/build_graph for external deps. Resolver-domain checks: **RES-WS-OVERRIDE-MEMBER-COLLISION** (override name == member), **RES-WS-MEMBER-REF-UNKNOWN** (`member "X"` no such member), **RES-WS-NO-INDEX** (member named dep, no index/override/member). (3) **`resolve_workspace_frozen`** (frozen.rs): per-member on-disk identity check → **FROZEN-MEMBER-IDENTITY-DRIFT**, lock member ∉ workspace → **FROZEN-MEMBER-NOT-IN-WORKSPACE** (both added to all_codes); members stay in place (no `_deps` symlink), externals from CAS. (4) **Per-member nim.cfg** (nimcfg.rs): `member_dep_closure` (toposort from a member, excl. self) + `format_workspace_nimcfgs` → `Vec<(member_path, text)>` with POSIX-relative `--path:` lines (member→member via `../<other>/src`, member→external via `../_deps/<name>`), NO root nim.cfg (lockfile §7.6). (5) **Harness:** `Produced::WorkspaceOutputs{lock_text, member_nimcfgs}` + `diff_workspace_success` (byte-diff shared milpa.lock + each `expected/<path>/nim.cfg` + `_deps_structure.txt`); `MilpaTarget` Resolve+Frozen route `ManifestDoc::Workspace` through `load_workspace(fx.dir)` (NOT the trait's `deps_dir.parent()` — the harness's scratch `_deps/` is detached from fixture inputs) + the free `resolve_workspace`/`resolve_workspace_frozen`. The `Resolver`/`FrozenResolver` trait `resolve_workspace*` impls on `Milpa` load from `deps_dir.parent()` (real project layout) — no longer `unimplemented!()`. **`E2E-NOT-WIRED` sentinel deleted** — all three cmd paths (parse-lockfile/resolve/frozen, single+workspace) now delegate to real milpa-core. `Strategy` + `resolve_workspace`/`resolve_workspace_frozen`/`format_workspace_nimcfgs` re-exported. **Greened 7 fixtures:** 085/086 (ws-frozen), 100/101/113 (RES-WS-*), 117 (ws-two-member-success — per-member nim.cfg byte-exact), **plus 062-diamond-conflict** (SOLVE-CONFLICT, now reachable through the fully-wired resolve path — the S7a "greens at S11" note was right). **milpa-core 101** (model change kept test count; +seed_workspace/closure logic) / others unchanged; clippy `-D warnings` + fmt clean; **corpus 115 pass / 2 xfail / 0 xpass / 0 regressions** (was 108/9). **Only 2 fixtures remain parked:** 099-res-provenance-conflict (needs a transitive mocked fetch to reach the two-non-root-claims path) + 115-conditional-dep-excluded (needs `Profile` platform/arch fields wired into predicate eval) — both non-blocking edge cases, addressable in S12+ or a small follow-up.
- **S11a result (DONE+GREEN, rust 14ce9fb):** **workspace member loader + structural validation** (S11 split a/b to de-risk the largest slice — a is the load-time structural layer, b is the resolve/frozen/emit layer). New `milpa-core/src/workspace.rs` = `load_workspace(root) -> LoadedWorkspace` (mirrors `workspace.py:load_workspace`): reads root `milpa.kdl` (must be a Workspace doc, else **WS-NOT-A-WORKSPACE**; missing → **WS-NO-MANIFEST**), then each declared member's package manifest from `<root>/<path>/milpa.kdl` in declaration order, validating topology: member `"."` → **WS-MEMBER-DOT**, no dir → **WS-MEMBER-DIR-MISSING**, no milpa.kdl → **WS-MEMBER-NO-MANIFEST**, member is itself a workspace → **WS-MEMBER-IS-WORKSPACE**, member has its own `overrides{}` → **WS-MEMBER-HAS-OVERRIDES**, two members same name → **WS-MEMBER-DUPLICATE-NAME**. Returns `LoadedWorkspace { root, members: Vec<LoadedMember{name,path,directory,manifest}>, overrides }`. (Orphan-member warning skipped — non-fatal, no warnings channel, not fixture-expressible. Discovery `find_workspace_root`/`workspace_containing` → S13 CLI; the harness knows the root.) **CoreError gained a `Workspace(&'static str, String)` variant** + 8 WS-* slugs in all_codes(). `load_workspace`/`LoadedWorkspace` re-exported. **Harness `MilpaTarget::Resolve` Workspace arm** now calls `load_workspace(fx.dir)` → surfaces WS-* / falls through to NOT_WIRED on a structurally-valid workspace (resolve_workspace is S11b). **Greened 6 WS fixtures:** 104-member-is-workspace, 107-member-dir-missing, 108-member-dot, 109-member-no-manifest, 110-member-has-overrides, 111-member-duplicate-name. Tests: +9 milpa-core workspace_tests (each WS code + a valid two-member load). **milpa-core 101** / others unchanged; clippy `-D warnings` + fmt clean; **corpus 108 pass / 9 xfail / 0 xpass / 0 regressions** (was 102/15).
- **S11b plan (NEXT):** `resolve_workspace(loaded_ws, index, fetcher, profile, prior, strategy, deps_dir)` — pre-register each member as a Candidate (member provenance, identity = `compute_content_hash(member.directory)`, NO fetch), seed root with member terms + member-coercion (a Named/Member dep whose name matches a member → member ref, not index lookup), reuse the S7b BFS/gate/finalize/build_graph for external deps, with the RES-WS-* resolver checks: **RES-WS-NO-INDEX** (named dep, no index, not a member/override), **RES-WS-OVERRIDE-MEMBER-COLLISION** (override name == member name), **RES-WS-MEMBER-REF-UNKNOWN** (`member "X"` with no member X). `resolve_workspace_frozen` (mirror frozen.py workspace path): per-member on-disk identity check → **FROZEN-MEMBER-IDENTITY-DRIFT**, lock member not in workspace → **FROZEN-MEMBER-NOT-IN-WORKSPACE** (add both to all_codes). Per-member nim.cfg via `member_dep_closure` + `format_workspace_nimcfgs` (relative paths member→member/external; lockfile §7.6 — per-member `expected/<path>/nim.cfg`, NO root nim.cfg). **Model change required:** workspace members emit `ProvenanceRecord::Member` in the lockfile, but `ResolvedDep.provenance` is the 4-kind transport `Provenance` (no Member — S5a deliberately separated transport from record). **Decision: change `ResolvedDep.provenance: Provenance` → `ProvenanceRecord`** (the post-resolution record IS emission-level; from_graph's transport→record map moves to resolver `build_graph` + simplifies from_graph to a clone; frozen `resolved_from_locked` simplifies to a record-clone; members → `ProvenanceRecord::Member`). Touches milpa-types + resolver build_graph + from_graph + frozen + nimcfg_tests/lockfile-tests/resolver_tests assertions (Provenance::→ProvenanceRecord::). Harness: route `ManifestDoc::Workspace` through resolve_workspace + per-member emitter (new `Produced::WorkspaceOutputs{lock_text, member_nimcfgs: Vec<(path,text)>}` + diff_success per-member). Greens 085,086,100,101,113,117. (Still parked after: 062-diamond-conflict=SOLVE-CONFLICT narration, 099-res-provenance-conflict=needs transitive mocked fetch, 115-conditional-dep-excluded=profile platform-field wiring — all S12+ / non-blocking.)
- **S10 result (DONE+GREEN, rust e48caee):** **single-package frozen path.** New `milpa-core/src/frozen.rs` = `resolve_frozen(manifest, lock, store, deps_dir) -> ResolvedGraph` (mirrors `frozen.py:resolve_frozen`): bypasses fetch+parse+solve, reconstructs the graph from the lockfile + CAS. Order-faithful to the oracle: `check_strategy` (default maxver vs `lock.strategy` → **FROZEN-STRATEGY-MISMATCH**) → `check_manifest_alignment` (every manifest dep in lock → **FROZEN-MANIFEST-DEP-NOT-IN-LOCK**; Named-dep constraint vs locked version, unparseable → **FROZEN-LOCKED-VERSION-UNPARSEABLE**, unsatisfied → **FROZEN-CONSTRAINT-UNSATISFIED**) → per-dep: Member provenance → **FROZEN-MEMBER-DEP**, Local → **FROZEN-LOCAL-DEP**, then `link_external` (identity not in CAS → **FROZEN-IDENTITY-NOT-IN-STORE**, else `store.link` symlinks `_deps/<name>`→CAS) → `resolved_from_locked` (maps the lockfile `ProvenanceRecord`→transport `Provenance`; Registry record → **FROZEN-LEGACY-REGISTRY-PROVENANCE**, raised AFTER the link, matching the oracle so 114's cas-seed is reached). The default-maxver detail: the `FrozenResolver` trait carries no strategy param (CLI concern, S13), so `Strategy::default()` is used internally. **CoreError gained a `Frozen(&'static str, String)` variant** + 8 FROZEN-* slugs in all_codes() (the 2 workspace-member codes land in S11 — subset rule). `FrozenResolver` impl on `Milpa` delegates here; `resolve_workspace_frozen` stays `unimplemented!()` until S11 (needs the workspace member loader). **Harness `MilpaTarget::Frozen` wired:** parse milpa.kdl (Package→frozen, Workspace→NOT_WIRED until S11) + milpa.lock, **seed the CAS from `cas-seed/`** (the S2-deferred copy-then-admit: each `cas-seed/<name>/` tree staged→hashed→admitted — new `seed_cas`/`copy_tree` helpers in runner.rs), then `resolve_frozen`; Ok→Outputs(from_graph+nimcfg), Err→code. `resolve_frozen` re-exported from milpa-core. **Greened 8 FROZEN fixtures:** 078-strategy-mismatch, 079-manifest-dep-not-in-lock, 080-constraint-unsatisfied, 081-member-dep, 082-local-dep, 083-identity-not-in-store, 106-locked-version-unparseable, 114-legacy-registry-provenance. (085-member-not-in-workspace + 086-member-identity-drift stay parked → S11 workspace-frozen.) Tests: +9 milpa-core frozen_tests (each FROZEN code + success-links-and-rebuilds) + 2 harness frozen tests (replacing the stale not-wired one). **milpa-core 92** / milpa-manifest 23 / solver 33 / types 6 / conformance 11+2+4 green; clippy `-D warnings` + fmt clean; **corpus 102 pass / 15 xfail / 0 xpass / 0 regressions** (was 94/23).
- **S9 result (DONE+GREEN, rust 718ad2c):** **nim.cfg emission + the CAS-materialization wiring that makes success fixtures green** (the first success path end-to-end: S4+S5b+S6+S7b+S7c+S8+**S9**). New `milpa-core/src/nimcfg.rs` = `format_nimcfg(graph, deps_dir, self_src_dir) -> String` (byte-exact mirror of `nimcfg.py:format_nimcfg`): normative 3-line header (SSOT'd as `const HEADER`), blank line, optional leading `--path:"<self_src_dir>"` (consuming pkg's own src, emitted first so self-modules shadow deps), one `--path:"_deps/<name>[/<src_dir>]"` per dep in **lexicographic name order** (§4.4 canonical, NOT toposort), trailing newline only when deps exist. POSIX separators always. Re-exported from milpa-core. **The load-bearing architectural decision:** the success `_deps_structure.txt` records only **symlinks** (`_deps/<name>` → `<CAS_ROOT>/sha256/<hash>/`), so resolve must CAS-admit + symlink. The resolve trait carries **no store parameter** (only `frozen` does), and Python puts the store inside the `FetcherRegistry(store=…)` — so **CAS materialization lives in the registry**, not the resolver. `FakeFetcher` (the harness's registry) now takes a `cas_root`, and its `fetch` stages the mocked bytes → `compute_content_hash` → `CaStore::admit` (move-via-rename, dup=no-op) → `CaStore::link` (relative `_deps/<name>`→CAS symlink). The resolver is UNCHANGED: its `fetch_any` still re-hashes `dest` for the identity gate, and `compute_content_hash` follows the symlinked root via `read_dir` (verified), so it independently verifies the CAS content (the "no fake can lie" property, RFC §4.6). **Harness `MilpaTarget::Resolve` success path** now builds `Produced::Outputs { lock_text: format_lockfile(from_graph(graph,"maxver")), nimcfg_text: format_nimcfg(graph,"_deps",manifest.src_dir) }`; `run_fixture` byte-diffs milpa.lock + nim.cfg + reads `_deps_structure.txt` from the materialized (symlinked) `_deps/`. **Deferred (documented, NOT silent):** per-member workspace nim.cfg (`write_workspace_nimcfgs`) → S11; feature-flag `-d:` defines (#23) → with the flag work that adds `active_flags`/`flag_defines` to `ResolvedDep` (no success fixture resolves a flag-carrying dep — same deferral as `from_graph`). **Greened 7 SUCCESS fixtures:** 003-single-url-dep, 061-named-dep, 063-canonical-selection, 064-dev-deps, 065-root-override-precedence, 116-src-dir-self-path, 118-lock-string-escaping. (Still parked: 062-diamond-conflict = SOLVE-CONFLICT narration, greens at S11 per S7a; 099-res-provenance-conflict needs transitive mocked fetch; 115-conditional-dep-excluded needs profile platform-field wiring; all frozen/ws → S10/S11.) Tests: +4 milpa-core nimcfg (single-dep+src_dir, no-deps-no-trailing-block, lexicographic order, self-src-dir-first) + FakeFetcher test updated to assert the CAS symlink. **milpa-core 83** / milpa-manifest 23 / solver 33 / types 6 / conformance 10+2+4 green; clippy `-D warnings` + fmt clean; **corpus 94 pass / 23 xfail / 0 xpass / 0 regressions** (was 87/30).
- **S8 result (DONE+GREEN, rust c1e2808):** **tianguis `index.kdl` reader + resolver rewiring + harness resolve wiring.** `registry.rs` fully rewritten (was a stub): `Index { packages: Vec<Package> }` with the richer `(namespace, name)` model — `Package { name, namespace, versions: Vec<IndexVersion> }`, `IndexVersion { version, content_hash, provenances: Vec<Provenance> }` (preference-ordered: canonical-then-mirrors, never reordered), `BareLookup` enum (Found/Ambiguous/NotFound — a typed result, NOT an exception, so the future multi-version provider can backtrack raise-free, mirrors Python's `lookup_bare`/`AmbiguousName`). `Index::parse` mirrors `tianguis_client.parse_index`: schema-version gate (`TNG-SCHEMA-UNKNOWN`, KDL-1.0 via `parse_v1` same as S3/S5a), eager safe-name validation of EVERY package node (`TNG-UNSAFE-NAME` — fires even for a package the manifest doesn't request, per fixture-097), per-provenance trust-boundary validators (`TNG-BAD-COMMIT-SHA` 40-lc-hex, `TNG-BAD-OCI-DIGEST` `sha256:<64hex>`, `TNG-UNSAFE-URL`/`TNG-UNSAFE-REF`/`TNG-UNSAFE-OCI-FIELD` leading-dash flag-injection), duplicate-version-keeps-first, unknown-provenance-kind skip (forward-compat), newest-first semver sort (parseable desc, unparseable trailing). `Index::resolve_named_all(name, &VersionSet, constraint_desc)` mirrors the Python resolve policy: `TNG-NOT-FOUND` / `TNG-AMBIGUOUS-NAME` / provenance-less-skip-then-`TNG-NO-PROVENANCE` / `TNG-NO-SATISFYING-VERSION`; content_hash is NOT checked here (that gate fires at fetch). `is_safe_name` is the SSOT safe-name predicate (registry-protocol §3.3). **Resolver rewired** (`resolver.rs`): `process_named` now calls `resolve_named_all` (eager TNG errors during Phase-A enumeration, replacing the old silent-empty `index_entries_for` which is deleted); `materialize_named` adds the **`TNG-NO-IDENTITY`** gate (empty content_hash → refuse before fetch — the S7b-deferred strictness) and fetches over `idx_ver.provenances` (ordered mirror fallback) recording the canonical provenance; stubs are now keyed to `IndexVersion`. **CoreError::all_codes()** gained the 12 TNG slugs (TNG-BAD-VERSION reserved/unraised by both impls → not listed; subset-valid). **Harness wired** (`runner.rs` `MilpaTarget::Resolve`): parse milpa.kdl (Package→resolve, Workspace→NOT_WIRED until S11), parse optional index.kdl (surfaces TNG-* parse errors), resolve via `Milpa::resolve` (default maxver) against a `FakeFetcher` over `mocked-fetches/`; clean resolve → NOT_WIRED (nim.cfg/lock byte-diff outputs are S9), error → its code. `FakeFetcher` now also impls `FetcherRegistry` (it mocks every transport from one tree, so it IS the registry in a fixture run). **Deferred (documented, NOT silent):** (1) the **4-state index network cache** (fresh/stale/offline-fallback/missing + `sha256(url)[..16]` key + `MILPA_INDEX_URL` + atomic write + `milpa clean` MUST-NOT-evict) → **S13**, where the CLI's index-loader (its only consumer) is built — no fixture exercises it (the harness reads index.kdl from disk), so building it now is consumer-less speculation ([[feedback_minimal_over_completeness]]); (2) the real dispatching `FetcherRegistry` (Provenance→per-transport Fetcher) → **S14**, where real transports exist to dispatch to. **Greened 13 fixtures:** the 12 TNG fixtures (087–098) + fixture-112-res-no-index (the wired resolve path now fires RES-NO-INDEX). Tests: +16 milpa-core registry tests (parse: sort/schema-gate/unsafe-name/bad-commit-sha/bad-oci-digest/leading-dash url+ref+oci/unknown-kind-skip/dup-version; resolve: not-found/ambiguous/no-satisfying/no-provenance/newest-first). **milpa-core 79** / milpa-manifest 23 / solver 33 / types 6 / conformance 10+2+4 green; clippy `-D warnings` + fmt clean; **corpus 87 pass / 30 xfail / 0 xpass / 0 regressions** (was 74/43). fixture-099-res-provenance-conflict stays parked (needs transitive mocked-fetches to reach the two-non-root-claims path; no regression).
- **S7c result (DONE+GREEN, rust 3f28406):** **emission glue — `from_graph(graph, strategy) -> Lockfile`** in `milpa-core/src/lockfile.rs` (+ `record_from_provenance` + `locked_from_resolved` + `opt`), re-exported from milpa-core. Bridges the resolver's `ResolvedGraph` → a canonical `Lockfile`: deps **sorted by name**, each dep's `requires` sorted **lexicographically** (resolver-semantics §4.4 / lockfile-schema §3.4 — the resolver accumulates both in topological/BFS order, not a stable cross-impl key, so the canonical ordering is imposed here, not in `format_lockfile` which stays a pure render per the S5b note). **Cleaner than the Python oracle:** Python's `_provenance_from_resolved` dispatches on a flat `source` string + typed `provenance`; the Rust `ResolvedDep` already carries the typed transport `Provenance` with its resolved commit/sha populated (set by `process_url`/`process_tarball`/etc. in S7b), so `record_from_provenance` is a **pure structural map** of the 4 transport arms → `ProvenanceRecord`: Git{url,ref_spec,commit_sha}→Git (ref_spec `""`→`None` via `opt`, honoring ProvenanceRecord's "None-when-omitted, never empty-string" invariant); Tarball{url,expected_sha256,..}→Tarball{url,sha256} (preserves the declared archive hash, lockfile.py M11); Local{path}→Local (the resolver already stores the *declared relative* path, not the abs fetch path); Oci→Oci. **`Member`/`Registry` records are unreachable from `from_graph`** — they have NO transport `Provenance` source, produced only by the workspace resolve (S11) and the legacy read path; noted in the doc-comment. **Deferred (noted, not silent):** `active_flags` (#23) / `self_mirrors` (#79) emitted **empty** — the `ResolvedDep` struct doesn't carry them yet and no current fixture resolves a flag-carrying or self-mirroring dep (matches Python's `getattr(d, ..., ())` default); threading them through the resolver lands with the feature-flag work that exercises them ([[feedback_minimal_over_completeness]]). Version rendered via `Version: Display` (the SSOT semver string; url-dep singleton `(0,0,1)`→`"0.0.1"`). Tests: +5 milpa-core (deps-sort + requires-lex-sort; strategy/singleton-version recording; all-4-transport-arms map; empty-git-ref→None; from_graph→format→parse round-trip with canonical dep order). **milpa-core 63** / milpa-manifest 23 / solver 33 / types 6 / conformance 10+2+4 green; clippy `-D warnings` + fmt clean; **corpus UNCHANGED 74 pass / 43 xfail / 0 xpass / 0 regressions** (S7c greens no fixture alone; first success = S4+S5b+S6+S7b+S7c+**S9** per §9 — S9 nim.cfg emission + S8 index reader complete the resolve→outputs path).
- **S7b result (DONE+GREEN, rust a6ce3bc):** **resolver orchestration** in new `milpa-core/src/resolver.rs` + new `.nimble` line-form parser in `milpa-manifest/src/nimble.rs` (mirrors `nimble_parse.py`; no-code-exec; hand-rolled, NO regex dep). `resolve(manifest, index, fetcher, profile, prior, strategy, deps_dir) -> ResolvedGraph` lands the `Resolver` impl on `Milpa` (the trait method uses `Strategy::default()`=maxver; the strategy override surface is S13). **Design = serial, two-phase.** The Python thread pool is dropped — spec §4.4 makes output `-j`-independent, so serial is a *more* deterministic conformant producer. URL/local/tarball deps materialize EAGERLY during a serial BFS; named (index) deps enumerate as lightweight stubs and fetch LAZILY the first time the solver's `dependencies()` selects a version (the RFC's "two-phase materializing provider") — so per-strategy selection fetches only the chosen version (proven by `resolve_named_dep_strategy_selects_version`). The provider impls `PackageProvider` over `RefCell` interior state; lazy-fetch errors are captured in a `RefCell<Option<MilpaError>>` and surfaced after `solve()` (the provider queries are infallible by trait signature). **Spec rules honored:** identity-singleton `Version(0,0,1)` for URL/local/tarball/member (§3); prior-lockfile pin reuse — git+ref match → `(identity, commit_sha)` from the same record, tarball url match → identity (§8); ordered mirror-fallback `fetch_any` = primary→dep-mirrors→prior self-mirrors, identity-gated, `FETCH-ALL-FAILED` if all fail (§8a); provenance precedence — root-authority first-claim wins/suppress transitive, two non-root claims → `RES-PROVENANCE-CONFLICT`, transitive `overrides{}` never read (§10); dev-deps — root enrolls deps+dev_deps, transitives read ONLY `manifest.deps` (the single §9 guard in `build_from_manifest`); content-hash dedup/alias to lex-min name for eager candidates (Phase B #32); profile predicate filtering with absent-profile=no-filtering (§6, against the minimal `Profile`: `nim`→nim_version, `flag`→flags; platform/arch/milpa need Profile fields → deferred to S9/fixture-115 wiring). `from_constraint` failures during resolve → `MAN-NIMBLE-CONSTRAINT`. **New codes wired+tested:** `RES-NO-INDEX`, `RES-PROVENANCE-CONFLICT` (CoreError), `FETCH-ALL-FAILED` (FetchError), `MAN-NIMBLE-CONSTRAINT` (MAN_CODES) — all subset-valid. **Deferred (noted, not silent):** `resolve_workspace` → `unimplemented!()` until S11; feature-flag cross-graph fixpoint union (#90) — not in resolver-semantics v1; named-dep `TNG-NO-IDENTITY` strictness → S8 (empty index content_hash currently admits freely); harness MilpaTarget resolve path NOT wired (success/named fixtures green at S9 with S8 index reader). Tests: +18 milpa-core resolver tests (ported from `test_resolver.py`: single/transitive URL, same-url dedup-fetch-once, named strategy max/minver, no-index error, url/named override, root-override-precedence suppression, non-root provenance conflict, dev-deps transitive-exclude, local copy+parse, fetch-failure surfaces, malformed-nimble-constraint, pin accept/reject, mirror fallback, content-hash dedup, profile filter + absent-profile) + 9 milpa-manifest nimble-parser tests. milpa-types 6 / milpa-solver 33 / **milpa-core 58** / **milpa-manifest 23** green; clippy `-D warnings` + fmt clean; **corpus UNCHANGED 74 pass / 43 xfail / 0 xpass / 0 regressions** (S7b unblocks URL-dep success fixtures; first success greens at S9 per §9). `tempfile` added as a milpa-core dev-dependency.
- **S7a result (DONE+GREEN, rust 63cf37c):** **solver core wired to the `pubgrub` 0.4.0 crate** (S0(b) decision honored — port the *contract*, not the hand-rolled loop). New in milpa-solver: a `PackageProvider` trait (`versions`/`dependencies`, mirrors the Python protocol) + a `Dep{package,constraint}` edge type (positive-Term equivalent) + `solve(provider, root, root_version, strategy) -> Result<BTreeMap<String,Version>, SolverError>`. The `ProviderAdapter` impls `pubgrub::DependencyProvider` (`type P=String`/`V=Version`/`VS=VersionSet`/`M=String`/`Priority=Reverse<String>` = the S0(b) BFS-by-name order/`Err=Infallible`); `choose_version` filters provider versions to the live range then applies `pick_version` (Maxver=max / Minver=min / Semver=`pick_semver`: highest sharing the range lower-bound's major, unbounded⇒maxver, no-same-major⇒`None`→pubgrub conflict); `get_dependencies` merges duplicate deps via intersection into a `BTreeMap` (deterministic order). **`VersionSet` now impls `pubgrub::VersionSet`** (empty/singleton=`eq`/complement/intersection/contains + overridden full/union/subset_of to milpa's canonical algebra so set-equality stays exact) and gained a `Display` (mirrors `_format_set`, only the conflict reporter consumes it but the `VS:Display` bound makes it mandatory). **`Version` gained `Display` in milpa-types** (the SSOT semver string — `pubgrub`'s `V:Display` bound needs it; the orphan rule keeps it with the type, same justification as `Ord`/`Eq`); **`format_version_str` now delegates to `v.to_string()`** (audit-for-duplication — one rendering, not two). SOLVE-CONFLICT narration = `pubgrub::DefaultStringReporter::report` over the derivation tree (after `collapse_no_versions`); `SolverError` gained `Display`+`Error`. Tests: +13 milpa-solver (ported from `test_solver.py`: single-root, named-dep, maxver-highest, cross-package unify, diamond/missing-dep conflict narration *names* the package, cycle-no-hang, **backtracks-to-compatible** = the PubGrub-forcing case, minver-floor, semver lock/unbounded-fallback/cross-major-reject) + 1 milpa-types Display. milpa-types 6 / milpa-solver 33 / milpa-core 40 / milpa-manifest 14 green; clippy `-D warnings` + fmt clean; **corpus UNCHANGED 74 pass / 43 xfail / 0 xpass / 0 regressions** (fixture-062-diamond-conflict needs S8 index-read + S11 workspace too, so SOLVE-CONFLICT greens at S11, not here — "unblocks vs greens", §9).
- **`from_graph` DEFERRED S5b→S7c (re-scoped, clear-best):** the handoff previously said "wire from_graph in S6 once format_version_str exists." `format_version_str` now exists, BUT a faithful `from_graph`/`_locked_from_resolved`/`_provenance_from_resolved` (lockfile.py:183-272) reads `ResolvedDep` fields the Rust struct does NOT yet carry — `sha`/`ref`/`source`/`active_flags`/`self_mirrors` — and must handle the `Member` record kind (no transport `Provenance::Member` exists). Those fields are produced by the resolver, so `from_graph` belongs in **S7c emission glue** where `ResolvedDep` is enriched; writing it now against the under-specified struct would bake a divergent mapping I'd rewrite at S7 ([[feedback_minimal_over_completeness]]). `format_version_str` is ready for S7c to call. The 4 transport→record arms (Git/Tarball/Local/Oci) are mechanical; Member/Registry come from workspace/legacy paths.
- **S7 resume — solver core + resolver orchestration + emission** (RFC §6 S7a/S7b/S7c): **S7a** wire the `pubgrub` 0.4.0 crate (S0(b) decision: `DependencyProvider::prioritize` = BFS order P returning `Reverse(name)`; `choose_version` = strategy pick — maxver highest-in-range; canonical solve verified on fixture-063 X@2.0.0/Y@1.0.0/Z@1.0.0). The Python `solve()`/`Term`/`Incompatibility`/`ConflictChain`/`render_conflict_chain` are the teaching-clean reference — Rust uses the crate, so port the *semantics* (SOLVE-CONFLICT + a conflict narration) not the hand-rolled loop. **S7b** resolver orchestration: manifest→fetch→parse(.nimble via milpa-manifest)→build Terms (map `ConstraintError`→`MAN-NIMBLE-CONSTRAINT`)→solve; pin reuse (prior lockfile, resolver-semantics §8), mirror fallback, provenance precedence (fixture-099/065), dev-deps (fixture-064), `_URL_DEP_VERSION` singleton (0,0,1) for URL/local/member deps (resolver-semantics §3). **S7c** emission glue: `ResolvedGraph`→outputs incl. the deferred `from_graph` (sort deps-by-name + requires-lexicographically HERE per S5b note, then `format_lockfile`). First success fixture greens at **S9** (needs S4+S5b+S6+S7b+S7c+S9 per §9). Spec refs: `docs/spec/resolver-semantics.md`, `milpa/resolver.py` + `milpa/solver.py` oracles.
- **Resume (stage 3):** `/loop implement the next unimplemented RFC slice with /tdd …` — next = **S3 KDL reader + manifest grammar** (RFC §6 S3 + §4.5 S0(a) decision). In `milpa-manifest`: use `kdl` 6.7.1 as a parse-only impl detail (annotation via `KdlEntry::ty()`, value via `.value().as_string()`, line/col via `KdlError::diagnostics[].span`) to parse `milpa.kdl` package + workspace (`(url)` annotation, `when`/predicate blocks, feature flags, `dev-deps`, `spec-version`) into the milpa-owned `Manifest`/`Workspace` (NEVER re-export the kdl AST; no emission depends on kdl). **Plus the `.nimble` line-form compat parser** (4 `requires` forms, `srcDir`, `when`-block warning, drop `nim` req). Replace `parse_manifest`/`parse_workspace` `unimplemented!()`. Fan `ManifestError` into the granular `MAN-*` codes (S1 skeleton currently maps the generic parse variant→`MAN-KDL-SYNTAX`); update `ManifestError::all_codes()` to list every real MAN-* it can now emit. **Wire `MilpaTarget`**: the `Cmd::Resolve` arm starts parsing (manifest parse alone greens the 62 MAN-* *error* fixtures, which fail before fetch/solve); the `Cmd::ParseLockfile` arm stays unwired (S5a). As MAN-* fixtures green, REMOVE their ids from `rust/crates/milpa-conformance/known_failing.txt`. NIMBLE-* are **exempt** (not fixture-expressible; unit-test only). TDD: port the Python manifest grammar unit tests + drive the MAN-* conformance fixtures green.
- **S3 spec refs:** `docs/spec/manifest-grammar.md` (full grammar), `docs/spec/errors.md` (MAN-* slugs), `tests/test_man_code_triggers.py` (trigger table → fixture mapping), `milpa/manifest.py` + `milpa/nimble_parse.py` (Python oracle). RFC §4.5 (kdl-rs decision/seam) + §6 S3.
- **S2 result (DONE, rust a05697b):** harness in `milpa-conformance` = `urlkey` (§2.3.1 + @-edge) / `fixture` (discover + cmd + expected I/O, NO milpa parsing) / `fake_fetcher` (`FakeFetcher` impl of `milpa_core::Fetcher`) / `runner` (`Target` seam + `Scratch` + `run_fixture` byte-diff/`<CAS_ROOT>`-canonicalize + `MilpaTarget` incremental-wire). **Design note:** the §4.4 sketch's `FixtureContext{manifest:Option<Manifest>}` was realized as the **`Target` trait** (impl-under-test parses raw inputs itself) — the truest black box (§5), keeps the oracle from baking in the parser it validates; the harness layer does pure fixture I/O. Per-fixture `cargo test <slug>` filtering (rstest/datatest-stable) was **deferred** — fixtures are dirs with non-uniform contents (datatest matches files), so a single aggregating corpus `#[test]` with a grep-able xfail/xpass summary is the better fit; RED→green observability preserved via the summary counts. `TempCaStore`/`cas-seed` copy-then-admit deferred to **S10/frozen** (synthetic fixtures don't need it; `admit` is S4). Error-parity is **subset (⊆)** now, flips to **bijection** at S12. 117 real fixtures all parked in `known_failing.txt`.
- **P1–P4 (committed, main):** 6569b32 P1 fixture-117 + per-member nim.cfg; 8c489eb P2 `.nimble` SSOT unify; b37e3e0 P3 `_kdl_str` lockfile escaping; 00aa968 P4 dispatch-exemption + §5.1. Suite 906/6.
- **S0 spikes (DONE, decisions recorded in RFC §6 S0):** (a) **kdl-rs `kdl` 6.7.1 — USE CRATE** (annotation via `KdlEntry::ty()`, value via `.value().as_string()`, errors carry line/col via `KdlError::diagnostics[].span`). (b) **pubgrub-rs `pubgrub` 0.4.0 — USE CRATE** (canonical solve on fixture-063 = X@2.0.0/Y@1.0.0/Z@1.0.0; BFS order P = `prioritize` returns `Reverse(name)`; maxver = `choose_version` highest-in-range). No fallbacks → **S7a stays the ~3-day "wire the crate" branch**. Pinned: kdl 6.7.1, pubgrub 0.4.0, miette 7.6.0. Throwaway spike crate deleted.
- **S1 scaffold (DONE+GREEN, uncommitted, on `main` working tree as untracked `rust/` + `dev-rust`):** six-crate workspace (milpa-types/solver/manifest/core/cli/conformance), `Cargo.toml` workspace + pinned `[workspace.dependencies]`, **committed-ready `Cargo.lock`**, `rust-toolchain.toml` (1.96.0), `rust/.gitignore` (target/, .cargo/), `./dev-rust` wrapper (podman/docker, tty-guard, cargo-registry volume). milpa-types = data skeletons (Version/Provenance+cas_admissible/ResolvedDep/ResolvedGraph/Lockfile); solver = Strategy/VersionSet/parse_version stubs + SolverError; manifest = Manifest/Workspace/Profile/ManifestError; core = 3 resolver traits + Fetcher/FetcherRegistry/Receipt + MilpaError+From impls + identity/store/registry stubs; cli = bin stub; conformance = links core + CORPUS_REL. **`cargo build --workspace` + `cargo clippy -D warnings` clean; 8 unit tests pass; `uv run pytest` 906/6 green.**
- **COMMIT PLAN (awaiting Corey go):** (1) S0 RFC decisions → `main` (design/spec lives on main, §4.3). (2) cut `rust` from `main`. (3) S1 scaffold (`rust/` + `dev-rust`) → `rust`. (4) standing per-slice commits on `rust` for S2+.
- **RFC:** `docs/rfc-rust-port-design.md` (rounds 1+2; committed main `8375f84`; S0 decisions added, uncommitted).
- **§8 GATE: RESOLVED + COMMITTED** — R1–R11 spec reconciliations committed main `b49e21b`.
- **Branch:** still on `main`. `rust` cut at commit-plan step 2.
- **Dev image (untracked):** `rust/Containerfile` built = `ghcr.io/coreyleavitt/milpa-rust:1.96` (Tumbleweed + rustup-pinned Rust 1.96.0 + rustfmt/clippy). NOTE deviation from RFC §4.3's "FROM rust:slim" wording — Tumbleweed+rustup bakes the toolchain at build time so the runtime build needs no network (the RFC's actual goal); immaterial base-image choice, not a spec violation. Committed at S1 with the workspace, on `rust`.

## P1 RESOLVED — workspace nim.cfg is per-member, not single (approved, landed)
P1 (workspace success fixture) hit a latent gap: the conformance harness's
`_outputs` emits ONE root nim.cfg via the single-package `format_nimcfg`, treating
members as `_deps/<name>` external deps. But milpa actually emits **per-member**
nim.cfg via `write_workspace_nimcfgs` — each member dir gets its own nim.cfg with
relative sibling paths, and there is NO root nim.cfg. Verified output for the
fixture-117 two-member case:
- `member-a/nim.cfg` = header only (no deps)
- `member-b/nim.cfg` = header + `--path:"../member-a/src"`
The spec (conformance-fixtures.md §2 layout / §2.1.1 / §2.5) assumes a single
`expected/nim.cfg`. No workspace success fixture ever existed, so the gap was never
exercised. milpa.lock + _deps_structure.txt ARE correct as shared root outputs
(members are exempt from CAS materialization, so _deps_structure is empty here).

**Recommended resolution (clear-best, but amends frozen spec v1.0 → needs go/no-go):**
1. Workspace success fixtures express nim.cfg per-member: `expected/<member-path>/nim.cfg`,
   one per member (mirrors the member-subdir input layout). No root `expected/nim.cfg`.
2. Harness `_outputs`/runner: when parsed type is WorkspaceManifest, write per-member
   nim.cfg via `write_workspace_nimcfgs` into a scratch copy and byte-diff each against
   `expected/<member>/nim.cfg`. milpa.lock + _deps_structure.txt stay single shared root files.
3. Amend conformance-fixtures.md §2/§2.1.1/§2.5 to define per-member nim.cfg for workspace fixtures.
4. Update RFC P1 text ("expected/{milpa.lock,nim.cfg,_deps_structure.txt}") + S11 + §9 coverage to match.
Fixture-117 inputs already authored (root + member-a/liba + member-b/libb→member liba).
Once approved: finish harness change, generate expected/, green Python suite, then resume P2.

## P2 RESOLVED — NIMBLE-* not fixture-expressible + SSOT dup fixed inline (Corey: fix inline; DONE)
P2 said "author 2 NIMBLE-* error fixtures (coverage-floor bijection gap)". Investigation:
- **No bijection gap.** `NIMBLE-FILE-NOT-FOUND` has a direct unit test (test_error_catalog.py:923);
  `NIMBLE-FILE-UNREADABLE` is already in that file's KNOWN_UNTESTED (line 941). Lint is clean now.
- **Not fixture-reachable.** `load_nimble` (sole raiser of NIMBLE-*) has NO production caller —
  only tests call it. The conformance harness never runs `.nimble` discovery (reads milpa.kdl text
  directly; cmds = resolve/parse-lockfile/frozen). And you can't commit a missing/unreadable file to
  git, so -NOT-FOUND/-UNREADABLE are intrinsically not fixture-expressible.
- **SSOT duplication found.** `_load_manifest_from_nimble` (the real discovery path) re-reads the file
  and raises `MAN-FILE-UNREADABLE`; `load_nimble`(→NIMBLE-*) is dead outside tests. Two codes, one condition.

**Recommended resolution:**
1. **P2 = exemption, not fixtures** (mirror P4): document NIMBLE-FILE-* as CLI-discovery file-IO errors
   exempt from the conformance corpus — covered by unit tests + KNOWN_UNTESTED. Add to both impls'
   bijection-lint exemption lists (Python already effectively exempts via KNOWN_UNTESTED; document for Rust).
   Update RFC P2 text + §9 (NIMBLE-* row already says "none exist (corpus gap)" → change to "exempt").
2. **File a separate cleanup issue** for the load_nimble vs MAN-FILE-UNREADABLE duplication (catalog-design
   call: delegate `_load_manifest_from_nimble`→`load_nimble` and pick one code, OR retire load_nimble+NIMBLE-*).
   Touches errors.md (frozen v1.0) + both catalogs → not inline corpus-prep work. Per [[feedback_defer_file_now]] file now.

## Round-2 changes applied (clear-best)
- **CRITICAL** fixed: VersionSet/Strategy+algebra moved to milpa-solver (orphan-rule); milpa-types = raw Version + data only. Added type-placement table.
- **CRITICAL** fixed: pubgrub seam = `DependencyProvider::prioritize` (order P), NOT VersionSet trait; S0(b) criterion = solution-match on fixture-063, not emission order.
- Resolver trait gains `prior_lockfile` param (pin reuse). Provenance → closed enum (not dyn Any). Traits+MilpaError+From-impls live in milpa-core. build.rs→`#[test]` for bijection lint. BTreeMap/IndexMap determinism non-negotiable. FixtureContext builder seam + canonicalize-not-readlink trap. known_failing xpass detection. Containerfile base-image-with-toolchain + MSRV≥1.74.
- Slices: S5→S5a(parse,no-S4)/S5b(emit); S7b depends on S6; "unblocks vs greens" labels fixed; S13 now all 8 verbs incl add/remove/update + format_manifest; S14 strip-before-hash test; per-slice unreachable-code unit tests. §9 coverage map corrected (first success = S4+S5b+S6+S7b+S7c+S9).
- New §10 **pre-grind P-slices** (Corey's call: make them the FIRST slices, not side-channel issues): P1 workspace-success fixture, P2 NIMBLE-* fixtures, P3 escaping fixture+Python fix, P4 exclusive-dispatch exemption decision. (strip_components folded into S14, not a corpus slice.) Fixed factual error: MAN-*=62 not ~65; NIMBLE-* fixtures=0.
- Spec doc: lockfile §7.4 cross-ref to always-on header (R5 follow-up).

## Stage-3 order (when grind begins)
P1–P4 (on `main`) → S0 spikes (kdl-rs, pubgrub-rs) → S1 scaffold → S2 harness+self-test+CI → S3… per §6. First success fixture greens at S9.

## Open items for Corey
1. ~~Commit gate~~ — DONE: RFC+handoff (`8375f84`) + 3 spec docs (`b49e21b`) committed to main.
2. Containerfile push to ghcr-public deferred (needs `podman login ghcr.io` + push + flip package visibility). Not blocking.

## Constraints from Corey (verbatim intent)
- Rust impl developed on a **separate branch** (`rust`); merge to main when green.
- **Same-repo coexistence for now** — design how Python + Rust live side by side in this repo.
- Do **NOT** design a multi-repo conformance harness yet (may split repos later; not worth it now). One shared fixture corpus, read from disk by both impls.

## Key decisions in the draft (to be stressed in architect rounds)
- Pure Rust (no PyO3) — the reference must be an independent oracle.
- Layout: `/rust/` cargo workspace, crates `milpa-core` (lib) / `milpa-cli` (bin) / `milpa-conformance` (harness). Hatchling only packages `milpa/`, so Python build is unaffected. Fixtures referenced by relative path (one copy).
- The **fixtures** are the single source of truth; two runners (Python pytest + Rust `milpa-conformance`) consume one corpus.
- Library forks-with-fallback: `kdl` (kdl-rs) for parse only / hand-roll fallback; `pubgrub` (pubgrub-rs) or port the teaching solver; `sha2`; real fetchers behind a trait (fake-injected in fixtures, not fixture-gated).
- Spec-conformance is the bar, not Python-parity (Rust may be *more* conformant, e.g. #117).

## Slices (15; see RFC §6)
S1 scaffold+coexistence · S2 conformance harness (RED backbone) · S3 KDL+manifest · S4 identity+CAS · S5 lockfile · S6 version/VersionSet/Strategy · S7 solver+resolver · S8 fetcher trait+fake+index/TNG · S9 nim.cfg · S10 frozen · S11 workspace · S12 error-catalog parity · S13 CLI · S14 real fetchers · S15 (stretch) differential harness.
Done = S1–S13 + full spec-v1 suite green under milpa-conformance.

## Open questions — ALL RESOLVED in round 1 (RFC §7)
- kdl-rs → S0(a) spike (annotation+value accessible; error carries line/col); hand-roll fallback.
- pubgrub-rs → S0(b) spike vs fixture-063; milpa VersionSet impls pubgrub trait; port fallback.
- harness → #[test]/rstest parametrization (not standalone bin).
- error parity → independent Rust catalog + build.rs lint vs errors.md.

## Round-1 changes applied to RFC (clear-best)
- Crate split 3→6 (milpa-types vocabulary crate enforces SSOT at compile time).
- 3 narrow traits (LockfileParser/Resolver/FrozenResolver) replace god `trait Milpa`.
- Fetcher returns receipt-not-identity; cas_admissible on Provenance. Per-domain error enums + MilpaError.code().
- Slices: added S0 spikes; split S7→S7a/b/c; S2 done = synthetic pass+fail self-test (not "all RED"); S14 gets local no-network tests; known_failing.txt drives progress; CI minimal by S2; toolchain pinned + containerized.
- Added missing-coverage acceptance criteria: verify verb (S13), index cache 4-state (S8), mirror fallback + pin reuse + provenance precedence + dev-deps (S7b), .nimble parse + TOFU (S3/S14), per-member nim.cfg closure (S11), env→Profile/_deps literal/member subdirs (S2).
- New §8 (11 spec reconciliations R1–R11 + workspace-success-fixture corpus gap) and §9 (fixture coverage map).

## GATE awaiting Corey (the one escalation)
§8: the review found 11 internal spec contradictions (R1/R2 text-verified: nim.cfg order conflict; _deps_structure relative-vs-absolute). All fixtures-win, v1-permitted reconciliations. Need go/no-go to land them on main (per §4.3 reflow) before the dependent slices.

## Context note
This RFC opened in the same session that froze spec v1.0 (prior RFC: rfc-reaching-rust-rewrite, committed: a2957b6/ad55aa0/20e2de6). Context is large — safe to `/compact` after architect round 1, or `/clear` before a fresh session (re-read this handoff first).

## Review ledger (stage 4)
| id | sev | finding | status | proof / reason |
|----|-----|---------|--------|----------------|
| —  | —   | (stage 4 not started) | — | — |
