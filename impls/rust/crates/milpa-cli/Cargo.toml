[package]
name = "milpa-cli"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[[bin]]
name = "milpa"
path = "src/main.rs"

[dependencies]
milpa-core = { workspace = true }
milpa-manifest = { workspace = true }
tempfile = "3"

[dev-dependencies]
# Test-only: craft tampered bundle fixtures for the S4b strict-fails integration test.
serde_json = "1"
