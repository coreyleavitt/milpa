# Conformance fixtures not yet greenable by the Rust impl (RFC §4.3).
# Format: one fixture id per line; '#' starts a comment; blanks ignored.
# These leave the list as their slices land; MUST be empty for v1-Done (§6).
# S2: MilpaTarget unwired — entire corpus parked.
# S3: manifest grammar wired — 62 MAN-* parse-error fixtures greened + removed
#     (001,002,004-060,102,103,105).
# S5a: lockfile-parse wired — 12 LOCK-* parse-error fixtures greened + removed
#     (066-077). 43 remain (resolve/frozen/tng/ws/res success+error paths,
#     pending S4/S5b/S6-S13).
#
# check-certificate: wired in batch 2 via --certificate CLI flag.
# Skipped in-process (CliOnly); verified by the black-box harness.
#
# Pre-existing baseline reds unrelated to #23 (discovered at Stage-3 baseline):
# fixture-099: FIXED (rfc-conformance-parity Slice 1) — edgeset_to_extracted no
#   longer dedups URL items by derived name before the provenance gate, so two
#   URLs stripping to the same name both reach gate() → RES-PROVENANCE-CONFLICT.
# fixture-144: FIXED (rfc-conformance-parity Slice 2) — the in-process adapter now
#   mirrors the CLI's MILPA_INDEX_URL→HttpDepDeclStore path, so an absent dep-decl/
#   with an index present raises TNG-DEPDECL-FETCH-FAILED (was RES-UNATTESTED-METADATA).
#
# known_failing is now EMPTY — every fixture greens in-process for Rust.
